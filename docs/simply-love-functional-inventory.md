# Simply Love Functional Inventory for VOLT26

## Purpose

This living document tracks non-visual Simply Love behavior that may need to be retained, adapted, replaced, or omitted as VOLT26 becomes a standalone ITGmania theme. It deliberately excludes purely decorative assets and styling. Mixed features are included when their source combines behavior and presentation.

## Status and decision vocabulary

**Decision**

- `Assess`: investigation or project-owner decision is still required.
- `Adopt`: retain the behavior with minimal semantic change.
- `Adapt`: retain the behavior but integrate it into VOLT26 architecture or UX.
- `Replace`: implement the same user need differently.
- `Omit`: intentionally exclude it and document the rationale.

**Implementation status**

- `Not started`
- `In progress`
- `Implemented`
- `Verified`

## Inventory entry requirements

Before an item is considered complete, record:

- the upstream Simply Love tag or commit reviewed;
- behavioral responsibility and user-visible result;
- source landmarks and important dependencies;
- persisted preferences or profile data;
- the VOLT26 decision and rationale;
- the implementation branch and VOLT26 destination;
- automated checks, manual verification steps, and result.

## Initial capability map

This capability map combines the completed source-level dependency scan with the standalone slices accepted to date. `Implemented` means that the recorded normal route works through a VOLT26-owned interface; it does not imply that every extended-coverage case has passed. Items still marked `Assess` require an explicit project-owner scope decision before migration.

| ID | Non-visual capability | Primary source landmarks | Decision | Status |
| --- | --- | --- | --- | --- |
| CORE-01 | Theme initialization and shared runtime state | `Scripts/SL_Init.lua`, `Scripts/06 SL-Utilities.lua` | Adapt | Verified |
| CORE-02 | ITGmania compatibility validation and startup failure handling | `Scripts/SL_Init.lua` (`VOLT26.Compatibility`), `BGAnimations/ScreenInit overlay/CompatibilityChecks.lua` | Adapt | Verified |
| CORE-03 | Theme preference definitions, defaults, loading, and saving | `Scripts/99 SL-ThemePrefs.lua`, `Scripts/SL_Init.lua` (`VOLT26.ThemePrefs`) | Adapt | Verified |
| CORE-04 | Screen branching and navigation decisions | `Scripts/VOLT26_Navigation.lua`, screen metrics in `metrics.ini` | Adapt | Verified |
| CORE-05 | Shared helper, text, UTF-8, and support utilities | `Scripts/06 VOLT26-Utilities.lua`, `Scripts/VOLT26_Text.lua`, `Scripts/VOLT26_UTF8.lua`, `Scripts/SL_Init.lua` (`VOLT26.Compatibility`) | Adapt | Verified |
| PROFILE-01 | Player profile discovery, selection, and profile-derived state | `Scripts/SL-PlayerProfiles.lua`, `BGAnimations/ScreenSelectProfile underlay/` | Adapt | Verified |
| OPTIONS-01 | Player option construction and persistence | `Scripts/SL-PlayerOptions.lua`, `BGAnimations/ScreenPlayerOptions*` | Adapt | Verified |
| OPTIONS-02 | Operator/service options and active option-row behavior | `Scripts/VOLT26_OperatorOptions.lua`, `Scripts/SL_Init.lua` (`VOLT26.Options`), `BGAnimations/ScreenOptionsService overlay/` | Adapt | Verified |
| SELECT-01 | Music selection state and helper logic | `Scripts/SL-SelectMusicHelpers.lua`, `BGAnimations/ScreenSelectMusic overlay/` | Adapt | Verified |
| SELECT-02 | Song search, filtering, sorting, and wheel behavior | `BGAnimations/ScreenSelectMusic overlay/SongSearch/`, `SortMenu/`, `Scripts/Consensual-sick_wheel.lua` | Adapt | Verified |
| SELECT-03 | Favorite-song persistence and handling | `Scripts/SL_Init.lua` (`VOLT26.Favorites`), music-selection and evaluation consumers | Adapt | Verified |
| SELECT-04 | Chart metadata parsing and derived chart information | `Scripts/SL_Init.lua` (`VOLT26.ChartData`, `VOLT26.ChartAnalysis`, `VOLT26.ChartHash`), `Scripts/SL-ChartParser.lua` | Adapt | Verified |
| SELECT-05 | Casual mode group, song, and option selection | Removed `BGAnimations/ScreenSelectMusicCasual overlay/` and `Other/CasualMode-*.txt` | Omit | Verified |
| GAME-01 | Custom score calculation and score-state management | `Scripts/SL_Init.lua` (`VOLT26.Scoring`), `Scripts/VOLT26_ScoreExport.lua` | Adapt | Verified |
| GAME-02 | Judgment-offset and per-column judgment tracking | `BGAnimations/ScreenGameplay overlay/JudgmentOffsetTracking.lua`, `PerColumnJudgmentTracking.lua` | Adapt | Verified |
| GAME-03 | EX judgment tracking and subtractive scoring | `BGAnimations/ScreenGameplay overlay/TrackExScoreJudgments.lua`, `BGAnimations/ScreenGameplay underlay/PerPlayer/NoteField/SubtractiveScoring.lua` | Adapt | Verified |
| GAME-04 | Failure behavior, fail timing, and hold-start failure rules | `BGAnimations/ScreenGameplay overlay/TrackFailTime.lua`, `FailOnHoldStart.lua` | Adapt | Verified |
| GAME-05 | Gameplay statistics, density, measure counters, and timing data | `BGAnimations/ScreenGameplay underlay/PerPlayer/StepStatistics/`, `NoteField/MeasureCounter.lua`, `Scripts/SL-Histogram.lua` | Adapt | Verified |
| GAME-06 | Target-score, pacemaker, and missed-target actions | `BGAnimations/ScreenGameplay underlay/PerPlayer/TargetScore/` | Adapt | Verified |
| GAME-07 | Course-song speed changes and inter-song behavior | `BGAnimations/ScreenGameplay next course song/` | Adapt | Verified |
| GAME-08 | Two-player comparison and current-winner calculation | `BGAnimations/ScreenGameplay overlay/WhoIsCurrentlyWinning.lua`, `underlay/Shared/VersusStepStatistics.lua` | Adapt | Verified |
| EVAL-01 | Evaluation state aggregation and cross-screen storage | `BGAnimations/ScreenEvaluation common/PerPlayer/Storage.lua`, `Shared/GlobalStorage.lua` | Adapt | Verified |
| EVAL-02 | Records, grade, percentage, judgment, and stream calculations | `Scripts/SL_Init.lua` (`VOLT26.Results`), `BGAnimations/ScreenEvaluation common/Panes/`, `PerPlayer/Upper/` | Adapt | Verified |
| EVAL-03 | Evaluation graphs, scatter plots, and derived performance analysis | `Scripts/SL_Init.lua` (`VOLT26.Analysis`), `BGAnimations/ScreenEvaluation common/PerPlayer/Lower/`, `Panes/Pane5/` | Adapt | Verified |
| EVAL-04 | Screenshot capture and evaluation exit/input handling | `Scripts/SL_Init.lua` (`VOLT26.EvaluationInput`), `BGAnimations/ScreenEvaluation common/InputHandler.lua`, `Shared/` | Adapt | Verified |
| ONLINE-01 | GrooveStats helpers, URLs, QR codes, and score submission | `VOLT26.GrooveStats`, `Scripts/SL-Helpers-GrooveStats.lua`, music/gameplay/Evaluation consumers | Adapt | Verified |
| EVENT-01 | Event progress and event-mode evaluation behavior | `VOLT26.Events`, `BGAnimations/ScreenEvaluation common/Shared/EventOverlay.lua`, `PerPlayer/Upper/EventProgress.lua` | Adapt | Verified |
| EVENT-02 | Tournament and ITL-specific behavior/files | `VOLT26.Tournament`, `BGAnimations/ScreenGameplay underlay/PerPlayer/TournamentMode.lua`, `Scripts/SL_ITL.lua` | Adapt | Verified |
| CONTENT-01 | Custom-song loading and removable-media workflow | `VOLT26.CustomSongs`, `Scripts/VOLT26_OperatorOptions.lua`, `Other/Documentation/CustomSongsFromUSB-README.md` | Adapt | Verified |
| SERVICE-01 | Input diagnostics and pad testing | `VOLT26.InputDiagnostics`, `BGAnimations/_modules/TestInput Pad/`, Service/Song Select/Evaluation consumers | Adapt | Verified |
| SERVICE-02 | Download-view navigation and item handling | `VOLT26.Downloads`, `BGAnimations/ScreenViewDownloads overlay/` | Adapt | Verified |
| SESSION-01 | Gameplay/session duration and game-over player statistics | `Scripts/SL_Init.lua` (`VOLT26.Session`), `BGAnimations/ScreenGameplay overlay/TrackTimeSpentInGameplay.lua`, `BGAnimations/ScreenGameOver overlay/` | Adapt | Verified |

## Phase 1 source scan progress

The source scan records what the inherited implementation did at the reviewed upstream baseline. Subsequent standalone branches may finalize a decision and advance implementation status independently of the scan result.

| Scan group | Upstream baseline | Coverage | Result |
| --- | --- | --- | --- |
| Core runtime | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `CORE-01` through `CORE-05` | All five capabilities are implemented and verified as VOLT26-owned services |
| Profiles and options | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `PROFILE-01`, `OPTIONS-01`, and `OPTIONS-02` | All three capabilities are implemented and verified behind VOLT26-owned profile, player-option, and operator-option boundaries |
| Music selection | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `SELECT-01` through `SELECT-05` | SELECT-01 through SELECT-04 are verified; SELECT-05 is verified as an owner-approved omission |
| Gameplay | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `GAME-01` through `GAME-08` | All eight Adapt decisions are implemented and verified as VOLT26-owned services; extended coverage remains documented per item |
| Evaluation | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `EVAL-01` through `EVAL-04` | All four Adapt decisions implemented and owner-verified on normal routes; extended coverage pending |
| Online services | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `ONLINE-01` | Accepted HTTPS GrooveStats profile and leaderboard route is owner-verified; submission variants remain extended coverage |
| Events and tournament support | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `EVENT-01` and `EVENT-02` | Generic Tournament Mode and the dormant validated-event boundary are accepted; annual providers remain explicitly outside the active scope |
| Removable-media content | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `CONTENT-01` | Operator preferences and persistence are owner-verified; physical-media variants remain engine-level extended coverage |
| Service and maintenance screens | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `SERVICE-01` and `SERVICE-02` | Both capabilities are implemented and owner-accepted behind VOLT26-owned boundaries |
| Session accounting | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `SESSION-01` | Implemented, engine-verified, and owner-accepted on the normal Game Over route |

## Core runtime assessment

### CORE-01 — Theme initialization and shared runtime state

- **Behavioral responsibility:** creates the global `SL` namespace, per-player state, per-game-cycle state, timing/scoring/life constants, EX weights, GrooveStats service state, request/download caches, and reset behavior. `InitializeSimplyLove()` resets both players and the game-cycle globals at script load and again when the title menu is entered.
- **Important dependencies:** `ThemePrefs`; `PREFSMAN`; `Branch.GameplayScreen()`; `CreateGrooveStatsPlayerOptionKeys()` from the GrooveStats helper; `LoadUnlocksCache()` from the GrooveStats helper; engine globals and enum helpers. Script load order is therefore part of the contract.
- **State boundaries:** player option selections, parsed chart caches, evaluation pane choices, API-session data, favorites, stage statistics, active rate, stage/continue counters, screen-return targets, timers, screenshot texture, and session timing are held in memory. Theme preferences and the GrooveStats unlock cache are read from persistent storage by dependencies; `SL_Init.lua` does not save them directly.
- **VOLT26 delta:** completed on `codex/standalone-core-completion`. `VOLT26` is the authoritative namespace with `Meta`, `State`, `Core`, and domain services. Initialization tolerates absent optional online helpers, and the title lifecycle resets transient game/player state through `InitializeVOLT26()`. `VOLT26.Preferences` temporarily retains inherited game-mode scoring data required by compatibility consumers. `SL` remains a declared adapter for screens outside the completed core slice; the obsolete `InitializeSimplyLove` alias was removed.
- **Mixed responsibilities:** visual color tables live beside scoring constants, online state, navigation state, and player/session data. These responsibilities should be separated during the standalone foundation work even if compatibility aliases are temporarily retained.
- **Recommended decision:** `Adapt` — retain reset semantics and the state required by accepted features, move it behind VOLT26-owned runtime modules, and remove state belonging to omitted features.
- **Verification status:** an engine-hosted contract run passed session reset checks for global, P1, and P2 state, including game mode, stage count, music rate, and API-session fields. It also confirmed that a persisted theme preference survives reset. A clean follow-up restart reached `ScreenTitleMenu` without Lua errors. Profile-owned data remains outside the reset boundary by design.

### CORE-02 — Compatibility validation and startup failure handling

- **Behavioral responsibility:** parses the engine version, accepts only ITGmania `1.3.0` or newer, accepts the `dance`, `pump`, `techno`, `para`, and `kb7` game types, and redirects unsupported configurations to `ScreenSystemOptions` with a localized system message. It also prevents the Thonk visual style from running under the Windows D3D render path that lacks render-to-texture support.
- **Important dependencies:** `ProductFamily()`, `ProductVersion()`, `GAMESTATE`, `HOOKS`, `PREFSMAN`, `SCREENMAN`, localized `ScreenInit`/theme-option strings, the `SM()` system-message utility, and `ScreenSystemOptions`/`ScreenThemeOptions` metrics.
- **State and persistence:** no direct persistence. The result depends on engine preferences such as the active game and video renderer.
- **VOLT26 delta:** completed on `codex/standalone-core-completion` and extended by `codex/standalone-core-utilities`. `VOLT26.Compatibility` owns version parsing, the ITGmania `1.3.0` minimum, supported-game policy, the current-environment check, theme metadata, and renderer capability policy. `ScreenInit` consumes that contract and produces one localized message and one safe redirect. The retained renderer guard and operator-support consumer use the same service; the inherited support-helper module has been removed. `_G.Volt26InitHandoff` remains a transient presentation handoff rather than compatibility state.
- **Mixed responsibilities:** the Thonk render-to-texture decision remains presentation-specific to the unassessed operator screen, while the underlying renderer-capability query is presentation-neutral and now lives in `VOLT26.Compatibility`. Theme-version and author values are metadata rather than startup-validation policy, but share the same read-only support boundary.
- **Recommended decision:** `Adapt` — retain the early engine/game guard, replace Simply Love wording and supported-version policy with a VOLT26-owned compatibility contract, and omit the Thonk-only branch unless independently required.
- **Verification status:** static checks passed, and a hidden bootstrap with `D:\SimplyLoveVanilla\Program\ITGmania.exe` loaded the preference registry, runtime, navigation graph, and `ScreenInit`, then reached `ScreenTitleMenu` without Lua errors. Minimum-version, older-version, and unsupported-game redirects remain extended compatibility-matrix coverage.

### CORE-03 — Theme preferences

- **Behavioral responsibility:** defines theme-wide defaults and option-row values, initializes fallback `ThemePrefs`/`ThemePrefsRows`, validates saved values by type and allowed-value membership, removes unknown keys, and saves the current theme section to `Save/ThemePrefs.ini`.
- **Important dependencies:** fallback-theme `ThemePrefs.InitAll()`, `ThemePrefsRows`, `IniFile`, date functions, localized strings, `range()`, `map()`, and `FindInTable()` from `06 SL-Utilities.lua`.
- **Persistent data:** navigation toggles; game/style/sort defaults; menu timers; color and visual-style choices; sampling, banner, keyboard, scoring, tournament, GrooveStats, unlock, QR-login, and lobby settings; edit-mode history; event state; and the temporary fixed `VOLT26` visual-style compatibility value.
- **VOLT26 delta:** completed on `codex/standalone-core-completion`. `VOLT26_Prefs` is the authoritative registry and `VOLT26.ThemePrefs` owns access, validation, game-mode application, and managed-engine-preference reset. A missing VOLT26 section imports recognized valid values once from `Simply Love` or `Simply-Love-SM5`; the incompatible legacy color is not imported, and the fixed `VisualStyle=VOLT26` adapter is enforced. Existing VOLT26 data always wins. `SL_CustomPrefs` remains a declared adapter only for the unassessed OPTIONS-02 service screen.
- **Mixed responsibilities:** a single registry couples core navigation and timing preferences to visual styles, optional online services, tournament behavior, and event unlocks. `VisualStyle` becomes obsolete when VOLT26 is no longer a Simply Love visual style.
- **Recommended decision:** `Adapt` — create a VOLT26-owned preference registry, preserve accepted keys with an explicit migration/default policy, split optional-feature preferences by domain, and retire `VisualStyle` checks from runtime branching.
- **Verification status:** the engine-hosted contract run confirmed the authoritative VOLT26 registry, current-value validation, fixed visual-style adapter, target-section precedence, reset persistence, and clean restart behavior. Missing-file, invalid-value, unknown-key, and legacy-import fixtures remain extended coverage.

### CORE-04 — Screen branching and navigation decisions

- **Behavioral responsibility:** selects profile/login/color/style/play-mode screens; applies game-mode preferences and reloads metrics; routes music/course selection, options, gameplay, evaluation, profile save, name entry, continues, game over, and title return; reconciles stage cost with music rate; and checks available credits for continues.
- **Important dependencies:** `ThemePrefs`, `SL.Global`, `GAMESTATE`, `PREFSMAN`, `PROFILEMAN`, `STATSMAN`, `SCREENMAN`, `THEME`, `MESSAGEMAN`, `GetCredits()`, `SetGameModePreferences()`, and fallback `Branch.AfterInit()`, `Branch.TitleMenu()`, and `Branch.GameplayScreen()` functions. Corresponding `metrics.ini` `PrevScreen`/`NextScreen` values form part of the route graph.
- **State and persistence:** mutates joined players/current style, `SL.Global.GameMode`, stage and continue counters, and player-option return targets. It reads persistent theme and engine preferences but does not save them.
- **VOLT26 delta:** completed on `codex/standalone-core-completion` and extended on `codex/paid-play-attract` and `codex/paid-song-select-timer`. `Scripts/VOLT26_Navigation.lua` owns the route graph, credit checks, stage-cost reconciliation, profile/music/options/gameplay/evaluation transitions, and end-of-session policy. Engine `Branch.*` callbacks and `SelectMusicOrCourse` are thin adapters. Navigation consumes VOLT26 state/services, forces the supported ITG ruleset without Visual Style branching, and treats `PreferredStyle=auto` as an explicit style-selection route. In paid play with zero credits and installed songs, the attract sequence routes the existing recent-score screens into ITGmania's native CPU demonstration before returning to the logo cycle. The left CPU uses normal scroll and the right CPU uses temporary Reverse scroll without changing persisted player options. The five-minute title-menu AFK video remains available outside paid play but is neither armed nor loaded on the arcade join screen. The standard menu countdown follows the machine-wide `MenuTimer` preference outside paid Song Select. Paid Mode always forces the Song Select countdown, preserves it across auxiliary screens, resets it after gameplay, and redraws it at the top center with the VOLT26 combo-number font in place of the Select artwork.
- **Mixed responsibilities:** optional-screen UX, coin-op policy, online login, stage accounting, profile persistence, gameplay mode selection, and end-of-session policy are combined in one module. Stage accounting belongs with session/gameplay behavior even though it currently controls navigation.
- **Recommended decision:** `Adapt` — define a VOLT26 route graph and explicit supported modes, retain only accepted coin/profile/session policies, and replace the temporary `VisualStyle` condition with standalone defaults.
- **Verification status:** the engine-hosted contract run passed VOLT26 route-service ownership, music routing, global/engine adapter identity, and one- and two-player profile/color-route checks. A clean normal startup reached the title screen, and the accepted profile, music, options, gameplay, and evaluation slices provide prior interactive coverage of the normal route. The paid-play extension passed a forced engine smoke test through `ScreenLogo`, both recent-score screens, and a random-song `ScreenDemonstration`; the dedicated demonstration actors loaded without gameplay telemetry or Lua errors. The project owner verified the live zero-credit route, AFK exclusion, asymmetric normal/Reverse CPU presentation, and enabled timer visibility on 2026-08-26. Disabled timer behavior awaits the final owner check. Course, cancel, continue, name-entry, and non-default game-over alternatives remain extended coverage.

### CORE-05 — Shared helpers, text, UTF-8, and support utilities

- **Behavioral responsibility:** provides generic table/system-message helpers (`TableToString`, `SM`, `range`, `map`, `deduplicate`, and lookup/string conversion), UTF-8 string primitives, emoji diffusion, multilingual wrapping/truncation, engine/metadata support helpers, and a large inherited `SL-Helpers.lua` collection.
- **Important dependencies:** Lua string/table APIs; StepMania actor types and global managers; fallback utility functions; `ThemePrefs`; and theme metadata lookup. `VOLT26_Text.lua` retains compatibility methods on `BitmapText`, so its load order and global effect remain part of the contract.
- **State and persistence:** generic utilities are stateless. Several functions in `SL-Helpers.lua` read or mutate engine preferences and runtime player state; `ResetPreferencesToStockSM5()` is state-changing and must not be treated as a harmless formatting helper.
- **VOLT26 delta:** on `codex/standalone-core-utilities`, `VOLT26.Util` owns deterministic table rendering, system messages, ranges, string conversion, lookup, mapping, and deduplication. `VOLT26.Text` owns emoji attributes and the UTF-8-aware BitmapText wrapping/truncation behavior, while `VOLT26.Compatibility` owns metadata and renderer checks. The utility, text, and UTF-8 modules now use VOLT26 filenames; the inherited support-helper module is removed. Early-load and unassessed-screen global utility adapters remain explicit compatibility boundaries.
- **Mixed responsibilities and inventory routing:** timing-window and worst-judgment helpers belong with `GAME-02`; EX counts and score calculation with `GAME-01`/`GAME-03`; game-mode preference application and stock-reset behavior with `CORE-03`; notefield geometry and asset lookup with implementation/presentation; course duration and stage helpers with `GAME-05`/session behavior. CORE-05 should retain only genuinely cross-cutting primitives.
- **Decision:** `Adapt` — keep the proven UTF-8/text behavior behind VOLT26 ownership, retain narrow early-load adapters, and leave timing, scoring, gameplay, options, and presentation helpers with their owning inventory items. The project owner authorized continued implementation of the next planned block on 2026-08-25.
- **Verification status:** an engine-hosted contract passed 25 of 25 utility, range, formatting, lookup, mapping, deduplication, recursive-table, UTF-8, text-adapter, version, metadata, and renderer-policy fixtures. A clean follow-up bootstrap loaded all renamed modules and reached `ScreenTitleMenu` without Lua errors. The project owner accepted the live CORE-05 verification on 2026-08-25. Malformed UTF-8, Japanese line-breaking edge cases, and renderer variation remain extended coverage.

## Profiles and options assessment

### PROFILE-01 — Player profile discovery, selection, and profile-derived state

- **Behavioral responsibility:** enumerates local profiles and their display metadata, reads recent theme modifiers for profile previews, discovers profile avatars, and coordinates guest, local-profile, and memory-card selection. The profile screen also handles joining and unjoining, coin consumption and refunds, duplicate-profile prevention for two players, default-profile selection, and the fast profile switch used from music selection.
- **Load and save contract:** the engine calls `VOLT26.Profile.LoadCustom` and `VOLT26.Profile.SaveCustom` through the `[Profile]` metrics; the inherited global names remain compatibility adapters. Loading resets player options and transient player state while preserving the current session's stage history, validates stored keys by datatype and valid option-row values, restores engine modifiers, resets disabled timing windows, and reapplies the operator-defined fail type. Saving writes the permitted custom modifiers and the engine `PlayerOptionsString`.
- **Important dependencies:** `PROFILEMAN`, `GAMESTATE`, `MEMCARDMAN`, `FILEMAN`, `PREFSMAN`, `IniFile`, `ActorUtil`, `ThemePrefs`, `CustomOptionRow()`, `GetDefaultFailType()`, `SL[pn]`, profile-screen engine index conventions, corresponding `metrics.ini` routes, and the GrooveStats and ITL read/write helpers.
- **Persistent data:** each persistent profile receives `<theme display name> UserPrefs.ini`, with a section also named from the theme display name. GrooveStats and ITL data are read and written as side effects of the same lifecycle. Engine-level default local-profile IDs and coin preferences also affect selection behavior. Guest state is transient, although stage history is deliberately retained across an in-session profile switch.
- **VOLT26 delta:** on `codex/standalone-profile-flow`, `VOLT26.Profile` owns the engine persistence callbacks, guest loading, avatar lookup, preferred-style access, and fast-switch state exposed to SCREEN code. ScreenSelectProfile no longer reads `SL.Global` or branches to SRPG presentation. Profile storage now derives its filename and section from the standalone VOLT26 theme identity.
- **Mixed responsibilities:** profile persistence is coupled to online-service and tournament files; profile selection is coupled to arcade credit policy, player joining, visual previews, and music-screen fast switching. These need explicit boundaries even if the existing screen behavior is retained initially.
- **Recommended decision:** `Adapt` — retain local, guest, and memory-card profile support plus validated per-profile modifiers, move persistence to an explicit VOLT26 schema with a documented Simply Love migration path, and make GrooveStats/ITL hooks optional integrations rather than mandatory profile side effects.
- **Verification status:** direct hidden startup of `ScreenSelectProfile` succeeds without Lua or asset-loading errors for one and two joined players. The project owner manually verified guest selection, two-player selection, local-profile selection, and cancellation. The boundary-completion contract additionally passed fast-switch lifecycle and idempotence checks, followed by a clean VOLT26 bootstrap. Memory-card profiles, duplicate selection, late join, coin consumption/refund, and persistence validation across restart remain extended-coverage cases.

### OPTIONS-01 — Player option construction and persistence

- **Behavioral responsibility:** builds custom option rows for speed, noteskin and variant, judgments, combo and hold graphics, notefield placement, music rate, chart selection, visibility, life display, score targets, timing feedback, gameplay statistics, and edit/attack modifiers. It translates selections between localized choices, `SL[pn].ActiveModifiers`, and the engine's preferred or stage `PlayerOptions`, then chooses the next options, music, or gameplay screen.
- **Application contract:** `CustomOptionRow()` exposes Lua option-row definitions to `metrics.ini`; generic load/save behavior is overridden where an option must mutate engine state or broadcast a preview/update message. `ApplyMods()` replays all rows after profile loading and on late join so saved settings apply even when the player never opens the options screen.
- **Important dependencies:** `SL[pn].ActiveModifiers`, `SL.Global.ScreenAfter`, `GAMESTATE`, `SCREENMAN`, `NOTESKIN`, `ThemePrefs`, `MESSAGEMAN`, chart/course APIs, gameplay-layout helpers, GrooveStats availability, localized strings, the three `ScreenPlayerOptions` metric groups, preview actors, and `PROFILE-01` for persistence.
- **State and persistence:** option changes update engine preferred/stage modifiers and in-memory theme state. Per-profile persistence is performed later by `SaveProfileCustom()`; music rate, current steps/trail, evaluation-pane defaults, and next-screen targets also mutate shared game-cycle state.
- **VOLT26 delta:** on `codex/standalone-options`, `VOLT26.Options` owns player/global modifier access, music-return eligibility, the shared options timer, and routing across the three Player Options pages. The dynamic speed-mod and noteskin-variant overlay uses this API, while page metrics no longer read `SL.Global.ScreenAfter` directly. The inherited option-row registry and profile persistence adapters remain pending further migration.
- **Mixed responsibilities and inventory routing:** the option framework currently embeds policy for tournament restrictions, online scoreboxes, chart selection, gameplay scoring displays, timing windows, course behavior, and screen navigation. The underlying feature decisions belong to their respective `GAME-*`, `ONLINE-01`, `EVENT-02`, `SELECT-*`, and `CORE-04` inventory items rather than being decided implicitly by the options UI.
- **Recommended decision:** `Adapt` — retain the proven per-player option lifecycle and profile application behavior, define a VOLT26-owned option registry, and include rows only for features accepted elsewhere in the inventory. Replace screen-specific actor manipulation with explicit update messages or interfaces where practical.
- **Verification status:** the project owner manually verified the normal Player Options flow after migration, including modifier interaction, navigation across the retained pages, and return routing. The boundary-completion contract passed available and locked music-return fixtures, followed by a clean VOLT26 bootstrap without Lua errors. Saved-option loading without first visiting the screen, full row-by-row persistence, localization, missing preview assets, routine, course, edit, late join, and restart scenarios remain extended coverage.

### OPTIONS-02 — Operator/service options and active option-row behavior

- **Behavioral responsibility:** defines the service-menu hierarchy and helper rows for theme selection, editor noteskin, default fail type, long/marathon thresholds, wheel speed, video renderer, global and visual offsets, memory cards, and custom-song limits. The service overlay supplies contextual help, remembers the active row when returning from child screens, saves theme preferences, rebuilds localized preference rows, and revalidates engine/game compatibility when leaving.
- **Important dependencies:** `PREFSMAN`, `VOLT26.ThemePrefs`, the temporary `ThemePrefsRows`/`SL_CustomPrefs` row adapters, `VOLT26.Options`, `SCREENMAN`, `GAMESTATE`, theme and engine metadata, `VOLT26.Compatibility`, localized strings, and the `ScreenOptionsService*` hierarchy in `metrics.ini`.
- **Persistent data:** engine preferences include default modifiers/fail type, editor noteskins, song-length thresholds, wheel speed, renderer order, timing offsets, memory-card behavior, and custom-song limits. Theme-owned rows persist through `ThemePrefs.Save()`. The remembered active option row is transient session state.
- **VOLT26 delta:** on `codex/standalone-operator-options`, `VOLT26.Options` owns the curated service-menu, theme-option, and timer-row policies plus bounded active-row restoration. `VOLT26.OperatorOptions` owns retained engine-preference rows under a VOLT26 filename and uses the shared utility contract. The menu initially hid optional integrations; `codex/standalone-groovestats` adds back a bounded GrooveStats child containing only its accepted master toggle. Tournament, Casual Mode, image-cache, alternate wheel-style, and visual-style choices remain hidden. The engine's stock theme selector replaces the inherited Simply Love switching/reset policy. The support actor saves theme preferences, rebuilds localized rows, validates the current environment, and removes its input callback on exit.
- **Mixed responsibilities:** engine-wide configuration, VOLT26 preferences, optional online/tournament features, theme switching, compatibility validation, and service-menu presentation share one hierarchy. Theme switching and renderer changes also have wider installation or restart consequences than ordinary theme preferences.
- **Decision:** `Adapt` — keep the engine settings that VOLT26 operators need, expose a smaller VOLT26-owned service hierarchy, separate machine preferences from theme and optional-integration settings, and remove visual-style selection from the standalone runtime. The project owner authorized continued implementation of the next planned block on 2026-08-25.
- **Verification status:** an engine-hosted contract passed 30 of 30 policy, conditional-row, accepted/hidden capability, timer, active-row, namespace, row-construction, evaluated-metric, and real service-overlay loading fixtures. A clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. The project owner accepted the curated Service Menu, child-screen return, and preference behavior on 2026-08-25. Renderer restart and invalid-environment redirection remain extended coverage.

## Music selection assessment

### SELECT-01 — Music selection state and helper logic

- **Behavioral responsibility:** initializes the standard music screen for a new gameplay entry, restores the selected music rate, reapplies profile or guest modifiers after joins and profile changes, preserves the menu timer across auxiliary screens, and automatically changes style so single, double, and routine charts can coexist in the wheel. Shared helpers start and stop casual-mode samples, position wheel title text, and derive the stage label from continues, song cost, and session progress.
- **Important dependencies:** `SL.Global`, `SL[pn]`, `GAMESTATE`, `PROFILEMAN`, `SCREENMAN`, `PREFSMAN`, `ThemePrefs`, `SOUND`, `THEME`, `LoadGuest()`, `ApplyMods()`, `generateFavoritesForMusicWheel()`, the option/profile lifecycle, and the `ScreenSelectMusic` metrics and engine music wheel.
- **State and persistence:** mutates the preferred song music rate, current style, transient gameplay-reload flag, current player modifiers, and preserved menu time. It reads theme and engine preferences but delegates durable player settings to `PROFILE-01` and `OPTIONS-01`.
- **VOLT26 delta:** on `codex/standalone-music-selection`, `VOLT26.MusicSelection` owns screen preparation, active-rate access, guest/profile refresh, and modifier reapplication. The later `codex/song-select-reset` presentation replaces the inherited wheel composition with a three-column VOLT26 layout: an engine-owned vertical wheel, a transparent timing-aware chart preview that is hidden on group rows, song and difficulty metadata, responsive one-/two-player chart panels, and player-name strips. Song Select uses the supplied `bg_ss@2x.png` background. The `codex/song-select-wheel-motion` presentation follow-up gives native song and pack rows the same short wheel transition, replaces the discontinuous focused-row gap with continuous spacing, and progressively fades only the outer rows. Right-side song and chart details now fade away during navigation, defer their data refresh until the selection has remained stable for 0.35 seconds, and return through a right-to-left slide and fade. The difficulty strip uses the same delay and enters from bottom to top. The chart-preview parser shares the settle delay so transient songs are neither read nor added to its caches during continuous scrolling, while the chart and its dark gradient backdrop reveal together as one layer. Production song-selection code no longer reads `VisualStyle`, and the componentized layout no longer reads the `SL` compatibility alias for music rate. Shared timer preservation, auto-style, favorites, sort, search, leaderboard, and input-test behavior remain inherited pending their respective decisions.
- **Recommended decision:** `Adapt` — retain the initialization and re-entry contracts, expose them through a VOLT26-owned music-selection controller, and replace the visual-style conditional with the standalone screen composition. Route stage accounting and player-option persistence through their owning modules rather than duplicating them here.
- **Verification status:** direct hidden loading reaches `ScreenSelectMusic` and loads `VOLT26MusicWheel`, the redesigned component layout, all pooled `cel` preview actors, responsive player panels, and native song rows without errors from the new files. The forced direct route still triggers the inherited sort-menu nil-style warning because ITGmania has no current style; the normal profile/style route is therefore required for final interactive verification of wheel geometry, audio synchronization, real chart parsing, one-/two-player reflow, courses, and re-entry. Earlier owner verification of the normal profile-to-music route and the music-rate boundary remains valid for the retained behavior.

### SELECT-02 — Song search, filtering, sorting, and wheel behavior

- **Behavioral responsibility:** drives the reusable Lua wheel, opens the sort/action overlay, changes engine sort order and preferred-song lists, switches regular/course play, game mode, and style, and routes to search, practice, profile switching, song reload, downloads, online lobbies, input testing, leaderboards, and evaluation summary. Search filters the current style's songs by title, transliterated title, pack, exact meter, and ten-BPM tier, then focuses the selected result and changes incompatible sort orders back to group sorting.
- **Important dependencies:** the engine `MusicWheel`, `SONGMAN`, `GAMESTATE`, `PROFILEMAN`, `SCREENMAN`, `MESSAGEMAN`, `PREFSMAN`, `ThemePrefs`, `SL.Global`, `SetGameModePreferences()`, profile playlist and favorites files, GrooveStats availability, localized prompt strings, and `Consensual-sick_wheel.lua` for the casual Lua wheels.
- **State and persistence:** mutates current sort, preferred-song source, play mode, game mode, style, current song/course, menu timer, and screen-routing flags. Profile switching explicitly saves player and machine profiles; the search query and candidate list are transient, while playlists and favorites are external profile files.
- **VOLT26 delta:** on `codex/standalone-song-browsing`, `VOLT26.SongBrowsing` owns search parsing, plain-text title/pack filtering, meter and BPM-tier filtering, preferred-result selection, sort broadcasts, playlist activation, and the accepted-action policy. The search actors and Sort Menu delegate to this interface, duplicate search logic has been removed, and direct `SL` state access has been removed from the path. Inherited online, event, practice, download, reload, and input-diagnostic handlers remain installed but are not exposed because their owning capabilities have not been accepted.
- **Decision:** `Adapt` — keep group/difficulty sorting, keyboard search, and the wheel interaction model; define a VOLT26-owned action registry whose entries are enabled only for accepted capabilities. Treat play-mode, profile, download, lobby, leaderboard, and evaluation routes as integrations with their inventory owners. The project owner confirmed this decision on 2026-08-24.
- **Verification status:** repository scans and static diff checks confirm one search implementation, policy guards on every unaccepted action, and no direct `SL` state access in the Sort/Search path. The project owner completed interactive verification of the retained Sort/Search flow and accepted the implementation on 2026-08-24. The boundary-completion contract passed pack, title, meter, BPM-tier, empty-query, accepted-action, and rejected-action fixtures. Extended coverage remains pending for very large result sets, transliterated titles, split BPM boundaries, locked wheels, timer transitions, course switching, profile playlists, and two-player focus.

### SELECT-03 — Favorite-song persistence and handling

- **Behavioral responsibility:** toggles the current song in a player's favorites, loads favorites when music selection starts or a profile changes, exposes them as an engine preferred-song list, and supports named sections in the text file while rewriting entries into a normalized, title-sorted form. Missing songs remain representable in the file but resolve to no song object at runtime.
- **Important dependencies:** `PROFILEMAN`, `GAMESTATE`, `SONGMAN`, `FILEMAN`, `RageFileUtil`, `VOLT26.Core` player state, profile-slot and player-number enum mappings, localized system messages, theme sounds, and the Sort Menu preferred-song integration.
- **State and persistence:** persistent profiles store normalized VOLT26-owned data in `VOLT26-favorites.txt`. When that file does not exist, the inherited `favorites.txt` format is imported without modifying the legacy file. Resolved songs and a normalized path lookup are cached per player; guest players have transient empty state and never write favorites.
- **VOLT26 delta:** on `codex/standalone-favorites`, `VOLT26.Favorites` owns path normalization, section parsing, deterministic serialization, legacy import, profile loading, membership queries, and toggling. It preserves missing-song paths, removes duplicate entries safely, rejects malformed path lines, reports write failures, and keeps presentation consumers behind a narrow interface. The inherited global handler and its direct `SL[pn].Favorites` access have been removed.
- **Decision:** `Adapt` — retain per-profile favorites and import the existing text format, but wrap parsing and writes in a VOLT26-owned persistence interface with explicit handling for malformed sections, missing songs, duplicate entries, write failures, and cross-theme ownership. The project owner confirmed this decision on 2026-08-24.
- **Verification status:** repository scans and static diff checks confirm that all five actor consumers use `VOLT26.Favorites`, the inherited global functions and direct `SL` favorite-state access are gone, and the legacy handler file has been removed. The project owner completed interactive verification and accepted the implementation on 2026-08-24. The boundary-completion contract passed mixed-separator path normalization plus CRLF and case-insensitive duplicate fixtures. Extended coverage remains pending for multiple named sections, malformed and missing-song paths, unwritable storage, and independent two-player persistence.

### SELECT-04 — Chart metadata parsing and derived chart information

- **Behavioral responsibility:** reads inexpensive engine-provided density, peak-NPS, and technical counts while browsing; parses `.sm` and `.ssc` note data for explicit consumers such as the Song Select note preview, GrooveStats-compatible hashes, and equal-spacing analysis; caches chart identity and results per player; reuses the opposite player's hash when safe; and derives column cues and human-readable stream/break sequences.
- **Important dependencies:** `Steps` engine APIs, `RageFileUtil`, `CRYPTMAN:SHA1String()`, difficulty and step-type enum helpers, `VOLT26.State`, and consumers in music selection, gameplay statistics, evaluation, column cues, and optional GrooveStats submission.
- **State and persistence:** parsed density, technique, hash, spacing, file identity, column-cue data, and preview note events live only in runtime state. Simfiles are read but never modified. Hashing deliberately caches by filename, step type, difficulty, and description; the visual preview caches file contents and per-player chart results, parses only the sample window in small measure- and row-bounded coroutine batches when its selected `Steps` identity changes, and renders through fixed actor pools. Duplicate layout refreshes preserve the active preview clock. Parser completion keeps the preview hidden for two initial render frames so CEL textures are prepared before a 0.3-second reveal, without restarting the preview clock.
- **VOLT26 delta:** on `codex/standalone-chart-metadata`, `VOLT26.ChartData` owns engine-backed metadata, normalized column-cue snapshots, and the fixed default Simply Love difficulty palette, while `VOLT26.ChartAnalysis` provides pure stream derivations. Music selection, gameplay, histogram, and evaluation consumers use these interfaces instead of inherited parser globals. The VOLT26 Song Select graph, textual difficulty, and numeric meter share the chart difficulty color. Player chart panels show technique in a dedicated `TECH` heading followed by the available compact counts (`FS`, `XO`, `SW`, `BR`, and `JA`). On `codex/song-select-reset`, a presentation-local parser reads only the sample window of the currently previewed `.sm`/`.ssc` chart, maps note rows through chart timing, applies CEL rhythm-color texture offsets from each parsed beat, and recycles fixed pools of CEL receptors, taps, mines, lifts, and hold actors. Parsing is delayed until wheel focus settles, divided across frames, and cached by chart/player to avoid blocking wheel navigation. The preview advances by timed beat distance at M600 rather than a fixed seconds-to-receptor window, so BPM changes, stops, delays, and warps remain visible; the declared display-BPM ceiling supplies the M-mod spacing reference so intentionally extreme hidden BPM gimmicks do not collapse the full preview into a note wall. Taps are culled as soon as they cross the receptor. Hold and roll actors use CEL heads, bottom-anchored repeating body textures, short texture-matched triangular tails, preview-edge clipping, and receptor-pinned active heads. It selects the higher-meter player chart, using the most recently changed player for equal meters. `VOLT26.ChartHash` remains a separate explicit API; preview parsing does not hash charts or broadcast online lookups.
- **Decision:** `Adapt` — retain engine-backed density and technique data for accepted displays, isolate optional hashing behind `ONLINE-01`, and expose immutable VOLT26 chart-analysis results rather than writing broad shared state. Keep stream and column-cue derivation only where an accepted consumer requires it. The project owner confirmed this decision on 2026-08-24.
- **Verification status:** repository scans and static diff checks on `codex/standalone-chart-metadata` confirm that accepted chart-data consumers use the three VOLT26 interfaces, inherited parser/helper globals are no longer consumed, and the music-wheel path does not call `VOLT26.ChartHash`. The project owner completed interactive verification of the retained chart metadata consumers on 2026-08-24. A hidden `codex/song-select-reset` load constructs the new parser and all `cel` preview pools without errors from the new files. Interactive verification of timing synchronization, taps, mines, lifts, holds, rolls, edits, split timing, couples/routine charts, malformed files, equal-meter selection, and dense-chart performance remains pending.

### SELECT-05 — Casual mode group, song, and option selection

- **Behavioral responsibility:** implements a separate group/song/options wheel for novice play, forces regular mode, filters configured groups to unlocked short songs with a chart at or below `CasualMaxMeter`, chooses a configured or fallback starting song, previews sample music, permits supported late joins, and lets each player choose chart, speed, and noteskin before both confirm gameplay.
- **Important dependencies:** `Other/CasualMode-Groups.txt`, `Other/CasualMode-DefaultSong.txt`, `SONGMAN`, `GAMESTATE`, `UNLOCKMAN`, `PREFSMAN`, `ThemePrefs`, `FILEMAN`, `RageFileUtil`, `SOUND`, `MESSAGEMAN`, `Branch.SSMCancel()`, player option objects, noteskin enumeration, credits/premium policy, and `Consensual-sick_wheel.lua`.
- **State and persistence:** mutates current play mode, song, steps, joined players, preferred speed modifier, and ready/focus state. Group and default-song policy is installation configuration; speed and noteskin persistence follows the normal player-option/profile lifecycle rather than being saved by the casual screen itself.
- **VOLT26 delta:** on `codex/omit-casual-mode`, the dedicated Casual music screen, configuration, and inherited documentation are removed. VOLT26 exposes ITG as its only game mode, routes regular song play through `ScreenSelectMusic`, and normalizes any stale in-memory `Casual` value to `ITG`. Casual preferences, menu-timer rows, Sort Menu actions, image-cache policy, and play-again/evaluation branches are no longer part of the product surface. Dead compatibility conditions inside shared gameplay and Evaluation actors remain unreachable and are reserved for the final adapter-removal pass.
- **Decision:** `Omit` — VOLT26 does not need a separate novice product mode. The project owner approved destructive removal of the dedicated screen and configuration on 2026-08-25.
- **Verification status:** repository contracts confirmed that only ITG is exposed by `ScreenSelectPlayMode`, regular play routes to `ScreenSelectMusic`, Casual preferences/actions and screen metrics are absent, banner caching is fixed to the retained policy, and all dedicated Casual files are removed. Hidden ITGmania launches remained responsive with the VOLT26 window active. The project owner accepted the normal profile/style-to-Song Select route and confirmed the omission on 2026-08-25.

## Gameplay assessment

### GAME-01 — Custom score calculation, export, and score-state management

- **Behavioral responsibility:** calculates Simply Love EX percentage from theme-owned judgment weights and chart radar totals, exposes current and final EX values to gameplay, evaluation, online, event, and tournament consumers, and writes one JSON score record per persistent human profile after evaluation. The exported record includes theme and engine identity, machine/player GUIDs, grade, engine score and percent DP, survival time, combo, modifiers, judgments, radar values, and song/steps or course/trail metadata.
- **Important dependencies:** `STATSMAN`, `GAMESTATE`, `PROFILEMAN`, `CRYPTMAN`, `PREFSMAN`, `RageFileUtil`, `JsonEncode()`, `SL.ExWeights`, `SL.Global.GameMode`, `VOLT26.Compatibility` theme metadata, chart radar APIs, `GAME-03` judgment counts, and evaluation's `WriteScores()` call. Course and song exports temporarily change `ShowNativeLanguage` to obtain native metadata, making restoration on every path part of the contract.
- **State and persistence:** live EX counts reside in the current per-player stage slot under `SL[pn].Stages.Stats`; `CalculateExScore()` returns a percentage plus actual and possible points without saving them. `WriteScores()` persists timestamp-named files under each profile's `SL-Scores/` directory and skips guests and autoplay, but the inherited writer does not surface directory, open, encoding, collision, or partial-write failures.
- **VOLT26 delta:** completed on `codex/standalone-gameplay-core-completion`. `VOLT26.Scoring` owns pure EX calculation from radar/count fixtures, W0 and hold normalization, failure-aware count mutation, detailed result judgments, and immutable live-score snapshots. `VOLT26.ScoreExport` owns a versioned `VOLT26.Score` schema, song/course metadata capture with guaranteed `ShowNativeLanguage` restoration, eligibility, collision-resistant filenames, JSON encoding, flush-on-close writes, and actionable error reporting. The inherited writer was removed; optional online/event modules retain narrow helper adapters until their own decisions.
- **Mixed responsibilities:** EX calculation, engine-score serialization, profile file I/O, online submission inputs, tournament weights, and presentation consumers share global functions and stage storage. Export ownership should be separated from scoring semantics, and optional online/event consumers should not define the core score schema.
- **Recommended decision:** `Adapt` — retain the accepted ITG/EX calculations and a durable per-profile score history, define a versioned VOLT26 schema and atomic/error-aware writer, keep engine and theme scores explicitly distinct, and expose immutable score snapshots to evaluation and integrations.
- **Verification status:** static scans passed. An engine-hosted contract run passed deterministic perfect, mixed, and zero-radar EX fixtures; judgment normalization; failure freezing; immutable and isolated 1P/2P score state; schema versioning; and JSON encoding. A clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. Real persistent-profile export, courses, no-mines, disabled windows, autoplay, and write failures remain extended coverage.

### GAME-02 — Judgment-offset and per-column judgment tracking

- **Behavioral responsibility:** listens to engine judgment messages without replacing engine scoring, stores chronological timing offsets and song/course positions for evaluation scatter plots and histograms, marks misses and automatic hold/checkpoint judgments, and counts tap judgments per physical column. Per-column data also distinguishes FA+ W0 hits, early judgments that were rescored, and misses caused by holding a panel.
- **Important dependencies:** engine `JudgmentMessageCommand` payloads and tap-note objects, `GAMESTATE`, `THEME`'s `RequireStepOnHoldHeads` metric, `IsW0Judgment()`, active FA+ modifiers, current style column count, course trail entries, and `SL[pn].Stages.Stats`. Evaluation panes, scatter plots, GrooveStats rescoring, and guest game-over statistics consume the captured data.
- **State and persistence:** both trackers accumulate in local tables during gameplay and copy them into the current stage's transient `SL` storage on screen exit. They do not write profile data directly; later evaluation or online flows may serialize or submit derived results.
- **VOLT26 delta:** completed on `codex/standalone-gameplay-core-completion`. Offset and per-column actors delegate capture to `VOLT26.Telemetry`; the service owns named offset records, normalized miss/autohit samples, guarded column indexing, and normalized per-column counts. Accepted Evaluation consumers use VOLT26 result/analysis snapshots. Compatibility fields remain only for unassessed submission and session consumers.
- **Recommended decision:** `Adapt` — retain offset and per-column capture for accepted evaluation and submission features, define a VOLT26 judgment-event schema with explicit column indexing and course timestamps, and isolate FA+/online-specific derived fields from the raw event stream.
- **Verification status:** the project owner previously verified normal gameplay capture and Evaluation output. The engine-hosted contract run passed miss, autohit, invalid-offset, immutable-snapshot, and independent 1P/2P state fixtures. Double, pump, routine, lifts, checkpoints, early rescoring, disabled windows, misses while held, rate mods, and multi-song courses remain extended coverage.

### GAME-03 — EX judgment tracking and subtractive scoring

- **Behavioral responsibility:** builds the live EX count table from tap and hold judgment messages, emulates the FA+ W0 window, maps FA+ timing tiers, treats missed holds as let-go judgments, freezes score-affecting counts after failure while preserving the full W0 total, and broadcasts recalculated EX values after each accepted judgment. The subtractive display reports early low-judgment counts and then percentage points lost from a perfect result, using either engine dance points or EX counts.
- **Important dependencies:** `STATSMAN`, `GAMESTATE`, active timing/scoring modifiers, `SL.ExWeights`, `SL.Metrics`, `CalculateExScore()`, `IsW0Judgment()`, notefield/layout helpers, combo fonts, top-screen life-meter access, and the `ExCountsChanged` message contract. Evaluation, event, online, versus, and step-stat consumers depend on these broadcasts and stored counts.
- **State and persistence:** EX counts are transient per-stage state. Subtractive-scoring counters are actor-local and reset for each gameplay screen, while courses deliberately retain them across songs. Durable persistence occurs only through downstream profile score export or online/tournament integrations.
- **VOLT26 delta:** completed on `codex/standalone-gameplay-core-completion`. Judgment normalization, failure freezing, post-failure W0 totals, score calculation, and immutable updates are owned by `VOLT26.Scoring`/`VOLT26.Telemetry`. Accepted live score, subtractive score, winner, and versus-stat consumers subscribe to `VOLT26ScoreChanged`; `ExCountsChanged` is broadcast only as a temporary adapter for unassessed online/event integrations. Gameplay exit reads the scoring service rather than compatibility stage fields.
- **Recommended decision:** `Adapt` — retain EX and subtractive scoring if they are part of VOLT26's target play experience, centralize judgment normalization and score calculation in one tested service, and publish a stable score-update event rather than recomputing or interpreting counts independently in multiple actors.
- **Verification status:** normal live scoring and final Evaluation data were previously owner-verified. The engine-hosted contract run passed tap/hold/W0 normalization, post-failure W0 accounting, score freezing, zero-radar handling, and two-player state isolation. Mines, no-mines, courses, disabled windows, sync prompts, and exact live 2P agreement remain extended coverage.

### GAME-04 — Failure, give-up, and fail-time behavior

- **Behavioral responsibility:** records the moment a player's health becomes dead, calculates song/course-relative fail position, stores graph placement and remaining-time labels, and annotates failures occurring inside a parsed stream. On gameplay exit it also detects give-up or premature termination, treats autoplay use as a failed attempt, handles early course exits, and explicitly fails all enabled players because the shared song stopped.
- **Important dependencies:** player health messages, `STATSMAN`, `GAMESTATE`, player song positions, course trail entries, active music rate, parsed `SL[pn].Streams`, current stage storage, and engine-version-dependent `StageStats:GaveUp()`. Evaluation graphs and fail descriptions consume the stored timing fields.
- **State and persistence:** failure timing and stream position are stored only in the current stage slot. The exit guard mutates engine player-stage stats by calling `FailPlayer()`; no direct profile file is written here, although the resulting failed status affects score saving and submission downstream.
- **VOLT26 delta:** completed on `codex/standalone-gameplay-core-completion`. `VOLT26.Failure` owns bounded failure snapshots, stream annotation, give-up detection, autoplay tracking, explicit attempt-failure policy, course early-exit handling, and per-player reconciliation results. Gameplay actors remain thin lifecycle adapters, and accepted Evaluation graphs consume named failure snapshots. Temporary flat fields remain only for inherited helper/session consumers; `FailOnHoldStart.lua` remains a loading-path adapter.
- **Recommended decision:** `Adapt` — retain explicit failure reconciliation and evaluation fail position, rename and isolate the give-up guard by responsibility, use the strongest engine API available behind a compatibility adapter, and define two-player and course failure policy explicitly.
- **Verification status:** the project owner previously verified normal completion and intentional failure, including the defeat route and Evaluation result. The engine-hosted contract run passed bounded failure snapshots and the normal, give-up, and autoplay exit-policy fixtures. Hold-start give-up, backing out, fail-off, immediate-fail, sync prompts, rate mods, stream annotations, courses, and asymmetric live failure remain extended coverage.

### GAME-05 — Gameplay statistics, density, measure counters, and timing data

- **Behavioral responsibility:** presents live tap, hold, mine, roll, score, and remaining-time statistics; parses per-measure chart density and stream/break sequences; draws static or scrolling NPS histograms and sampled life history; normalizes two-player graph height; and advances an optional current-measure counter with configurable stream thresholds and lookahead. Course transitions reparse each chart, reset per-song graphs and counters, and preserve cumulative course time.
- **Important dependencies:** `VOLT26.ChartData`, `VOLT26.ChartAnalysis`, `VOLT26.GameplayStats`, chart/trail radar and timing APIs, life-meter actors, `STATSMAN`, active gameplay modifiers, music rate, notefield/layout helpers, tournament preferences, and `CurrentSongChanged`/`PeakNPSUpdated` messages.
- **State and persistence:** immutable measure segments live in per-player VOLT26 gameplay-stat state and peak NPS values live in game-cycle state. Player-option strings are captured before forced song modifiers and restored later by music selection. Histogram vertices, bounded life samples, counters, and elapsed-time offsets remain screen-local. Display choices persist through profile options rather than these actors.
- **VOLT26 delta:** on `codex/standalone-gameplay-advanced-completion`, `VOLT26.GameplayStats` owns peak scaling, safe digit widths, immutable measure segments, stream/rest counter text, rate-normalized cumulative/remaining time, and bounded vertex interpolation. Histogram and gameplay-stat actors use these contracts instead of global environment keys or direct Simply Love gameplay state; presentation and the optional online scorebox remain separate consumers.
- **Decision:** `Adapt` — retain chart analysis, selected gameplay statistics, density/life graphs, remaining time, and measure counters behind VOLT26-owned data contracts while preserving the accepted presentation. The project owner authorized the GAME-05–08 implementation block on 2026-08-25.
- **Verification status:** an engine-hosted advanced-gameplay contract passed 35 of 35 combined fixtures, including empty and large counters, immutable stream segments, negative lead-in, course duration/rate, remaining-time clamps, peak normalization, and safe interpolation. A clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. The project owner accepted the live GAME-05–08 verification on 2026-08-25. Dense charts, sustained performance, and non-default styles remain extended coverage.

### GAME-06 — Target score, pacemaker, and missed-target actions

- **Behavioral responsibility:** derives a target percentage from a selected grade threshold or the top machine/personal score, falls back to an S threshold when no saved score exists, compares current dance points with the target throughout gameplay, broadcasts when the target becomes mathematically unreachable, and optionally shows a pacemaker or target/history graph. In Event Mode, the same signal can immediately fail the player or restart gameplay.
- **Important dependencies:** `STATSMAN`, `PROFILEMAN`, `GAMESTATE`, grade metrics, current player options, notefield/layout helpers, machine and player high-score lists, `PREFSMAN` Event Mode, `MESSAGEMAN`, `SCREENMAN`, and `Branch.GameplayScreen()`. The graph also consumes life and score history through its shared target setup.
- **State and persistence:** target selection and action preferences persist with player modifiers; personal and machine targets read engine high-score storage. Comparison state is actor-local. Fail/restart actions mutate stage stats and screen routing but do not save directly.
- **VOLT26 delta:** `VOLT26.TargetScore` owns fixed/best/fallback target resolution, normalized pacemaker precision, mathematical reachability, one-shot missed-target state, and Event-Mode-only fail/restart policy. Existing graph and pacemaker actors render this service and no longer derive the policy independently.
- **Decision:** `Adapt` — retain the optional pacemaker, target graph, machine/personal best targets, and explicit Event Mode fail/restart actions behind a VOLT26-owned policy. The project owner authorized the GAME-05–08 implementation block on 2026-08-25.
- **Verification status:** the engine-hosted contract passed fixed, machine, personal, missing-best fallback, normalized difference, unreachable-target, one-shot notification, and non-Event rejection fixtures. The project owner accepted the live GAME-05–08 verification on 2026-08-25. Very long charts, alternate target actions, centered layouts, and unusual course combinations remain extended coverage.

### GAME-07 — Course-song speed changes and inter-song behavior

- **Behavioral responsibility:** displays the next course song's background during the inter-song transition and temporarily accepts dedicated left/right menu input before the first judgment to adjust X, M, or C speed modifiers. It enforces positive and mode-specific upper bounds, applies the new modifier immediately, updates shared player state, refreshes the on-screen modifier display, and removes the callback at the first judgment or screen exit.
- **Important dependencies:** course transition messages, `GAMESTATE` song-level player options, `SCREENMAN` input callbacks, dedicated menu-button names, `VOLT26.Options`, `VOLT26.CourseSpeed`, `PlayerOptionsChanged`, active music rate, and background-loading actor APIs.
- **State and persistence:** the selected speed is written to transient active modifiers and engine player options. Profile persistence is indirect through the normal option/profile lifecycle; callback registration is screen-local and must be removed reliably between course songs.
- **VOLT26 delta:** `VOLT26.CourseSpeed` owns X/M/C discovery, adjustment bounds, increments, and GameCommand formatting. The inter-song actor accepts only dedicated menu buttons, registers its callback at most once, removes it at the first judgment or exit, updates VOLT26 modifier state, and fixes routine speed/mini command composition.
- **Decision:** `Adapt` — retain bounded pre-note course speed adjustment for X/M/C modifiers with explicit callback ownership and VOLT26 option persistence. The project owner authorized the GAME-05–08 implementation block on 2026-08-25.
- **Verification status:** the engine-hosted contract passed X/M/C increments, positive lower bound, upper bound, stable formatting, and invalid-kind rejection. The project owner accepted the live GAME-05–08 verification on 2026-08-25. Rapid simultaneous input, forced modifiers, unusual course entries, and two-player course persistence remain extended coverage.

### GAME-08 — Two-player comparison and shared versus statistics

- **Behavioral responsibility:** compares two players using the same selected scoring system and dims the losing score in real time, using precise dance-point ratios or the live EX update stream. For standard widescreen versus layouts it can also build a shared center statistics pane, reuse per-player judgment counters, add fallback score displays when other actors are obscured, scale background filters per player, and update the shared song banner.
- **Important dependencies:** `GAMESTATE`, `STATSMAN`, matching `ShowExScore` modifiers, named underlay score actors, `ExCountsChanged`, step-stat actors, tournament and banner preferences, background-filter helpers, notefield/aspect-ratio rules, and shared judgment/radar data.
- **State and persistence:** comparison ratios and display references are screen-local; the feature reads persistent player and operator choices but does not write them. Its accuracy depends on `GAME-03` live EX values or engine dance-point totals and on both players using comparable scoring.
- **VOLT26 delta:** `VOLT26.Versus` owns scoring-mode compatibility, zero-safe precise DP ratios, isolated two-player score state, and tie/leader resolution. The existing score-opacity and shared-stat actors consume this interface and VOLT26 modifier access; tournament and aspect-ratio presentation remain screen policy.
- **Decision:** `Adapt` — retain live DP/EX leader comparison and shared versus statistics when both players use the same scoring system. The project owner authorized the GAME-05–08 implementation block on 2026-08-25.
- **Verification status:** the engine-hosted contract passed matching/mismatched scoring, zero-possible DP, P1 lead, P2 lead, and tie fixtures. The project owner accepted the live GAME-05–08 verification on 2026-08-25. Hidden score actors, unequal charts, asymmetric failures, tournament mode, and non-default aspect-ratio layouts remain extended coverage.

## Evaluation assessment

### EVAL-01 — Evaluation state aggregation and cross-screen storage

- **Behavioral responsibility:** combines the engine's current `StageStats` with telemetry captured during gameplay and stores a session snapshot for the current song or course. Global storage records the played song/course and music rate; per-player storage adds profile identity, grade, percent dance points, EX score, aggregate judgments, chart/trail, difficulty, meter, author, enabled timing windows, and whether EX should be shown. Evaluation Summary pages through four stages at a time and reconstructs results for players who joined, left, or switched profiles during the session.
- **Important dependencies:** `STATSMAN`, `GAMESTATE`, `PROFILEMAN`, VOLT26 gameplay/session state, `VOLT26.Results`, active modifiers, `Graphics/ScreenEvaluationStage header.lua`, `ScreenEvaluationSummary overlay/`, and `ScreenNameEntryTraditional underlay/`. Gameplay allocates an explicit active-stage index; Evaluation completion advances the completed-stage count once even if the screen reloads.
- **State and persistence:** versioned global contexts and immutable per-player result snapshots remain in VOLT26's in-memory session state and feed Evaluation Summary, name entry, game-over statistics, and integrations. Compatibility aliases remain only for unassessed inherited consumers. The screen explicitly saves the machine profile on entry. Optional custom-score files are written before actor initialization by `GAME-01`; engine high scores and profile data are persisted through their own engine/profile lifecycle rather than by the snapshot actors.
- **VOLT26 delta:** `VOLT26.Gameplay` owns active-stage allocation, while `VOLT26.Evaluation` owns schema-versioned song/course contexts, immutable player snapshots, profile-history queries, and idempotent completion. Per-player storage is now a thin service call; Evaluation Summary and Name Entry consume the VOLT26 interfaces instead of direct Simply Love stage tables. Compatibility aliases preserve optional integrations outside the accepted boundary.
- **Recommended decision:** `Adapt` — retain multi-stage and profile-aware result history, but create one VOLT26-owned immutable result snapshot per completed stage, commit it at an explicit lifecycle boundary, and make summary/name-entry/export consumers depend on that schema rather than actor execution order.
- **Verification status:** the project owner completed normal-route pass and fail tests and verified populated Evaluation, Evaluation Summary, Name Entry, and Game Over routes. An engine-hosted boundary contract passed 26 of 26 combined checks, including shared active-stage identity, isolated and immutable 1P/2P snapshots, immutable contexts, profile-history deduplication, and idempotent completion; a clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. Courses, versus, routine, late join/unjoin, profile switching, guest profiles, live screen reloads, and four-plus-stage pagination remain extended coverage.

### EVAL-02 — Records, grade, percentage, judgment, and stream calculations

- **Behavioral responsibility:** presents engine percent dance points or theme EX percentage, aggregate tap/hold/mine/roll results, FA+ splits, per-column judgments, chart metadata, grade, stream/break performance, local machine and personal high-score lists, and record rank. Record detection sets the transient name-entry flag and includes a custom Event Mode fallback because the engine reports no machine high-score index there. The final-session summary redraws the stored score, grade, chart metadata, and judgment counts for every stage.
- **Important dependencies:** current and routine `PlayerStageStats`, engine radar actual/possible values, machine and player profiles, high-score list limits, grade metrics and assets, `SL.ExWeights`, `CalculateExScore()`, `GetExJudgmentCounts()`, per-column data from `GAME-02`, stream parsing from `SELECT-04`/`GAME-05`, active scoring and timing-window modifiers, and the subsequent name-entry route.
- **State and persistence:** record eligibility mutates `SL[pn].HighScores.EnteringName`; name entry later calls `GAMESTATE:StoreRankingName()` and updates the persistent profile's last-used name. Machine and personal score lists are engine-owned. Evaluation also triggers a machine-profile save, while the separate custom JSON export remains owned by `GAME-01`.
- **VOLT26 delta:** on `codex/standalone-evaluation-results`, `VOLT26.Results` creates one normalized current-result snapshot containing named DP and EX percentages, effective grade, engine and FA+ judgments, radar actual/possible values, per-column judgments, timing-window state, failure state, and display policy. It also centralizes machine/personal record eligibility, Event Mode fallback ranking, and the name-entry decision. Evaluation percentages, judgment panes, grade, record labels, high-score-list selection, stored summary data, the optional GrooveStats URL, and result easter eggs consume this interface instead of independently recalculating results. Compatibility summary tables remain populated for the existing multi-stage summary layout.
- **Decision:** `Adapt` — define a normalized VOLT26 result model and a single record/grade policy service, retain engine and EX percentages as distinct named values, and let panes, summaries, name entry, and integrations consume the same calculated result without duplicating eligibility or judgment-mapping rules. The project owner confirmed this decision on 2026-08-24.
- **Verification status:** repository scans and static diff checks on `codex/standalone-evaluation-results` confirm that the migrated Evaluation panes no longer call the inherited EX, grade, percent, record-index, or per-column compatibility calculations directly. A hidden bootstrap with `D:\SimplyLoveVanilla\Program\ITGmania.exe` loaded the VOLT26 scripts and reached the title screen without Lua errors; existing font-width warnings remain unrelated. The project owner completed interactive verification of the normal Evaluation route and accepted the implementation on 2026-08-24. Extended fixture coverage remains pending for disabled windows, holds, mines, rolls, failure, zero dance points, exact 100% EX, routine, courses, guest/local/memory-card profiles, Event Mode, empty and full score lists, ties at the cutoff, and profile switches.

### EVAL-03 — Evaluation graphs, scatter plots, and derived performance analysis

- **Behavioral responsibility:** overlays chart-density, life-history, timing-offset, and failure-position data; builds a chronological scatter plot with misses spanning the graph; and derives an aggregate timing histogram with mean absolute error, signed mean offset, sample deviation, and maximum error. Automatically judged pump hold/checkpoint events and misses are excluded from numeric timing statistics. Large scatter plots are split into batches to stay below renderer vertex limits.
- **Important dependencies:** chronological offsets from `GAME-02`, parsed chart density and NPS actors from `GAME-05`, fail position from `GAME-04`, engine `GraphDisplay` life data, song/trail timing APIs, music rate, timing-window and FA+ helpers, active modifiers, renderer behavior, and graph-size metrics. Course rendering additionally depends on cumulative trail duration and the duration actually survived.
- **State and persistence:** source timing events remain in the transient per-stage telemetry snapshot. `VOLT26.Analysis` returns copied derived data; histogram values and bounded vertex batches remain screen-local, and a single-player cache avoids rebuilding the same histogram for a second pane. The `StdDev` value now displays the calculated sample standard deviation (`σ`) in milliseconds rather than an unlabeled `3σ` value.
- **VOLT26 delta:** on `codex/standalone-evaluation-analysis`, `VOLT26.Analysis` owns timing-event selection, aggregate statistics with explicit millisecond fields, millisecond histogram smoothing, song/course timelines, and scatter coordinates. Misses and automatic hold/checkpoint judgments are excluded from numeric statistics. Evaluation graph actors consume named telemetry and failure snapshots instead of inherited positional stage fields, course scatter backgrounds use cumulative segments, and scatter points are clamped to graph bounds and split so each renderer batch stays at or below 16,000 vertices.
- **Decision:** `Adapt` — retain timing and life analysis, move derivation into tested pure functions with explicit units and course coordinates, name `σ` versus `3σ` accurately, bound retained data and vertices, and make each graph an optional consumer of a common analysis result. The project owner confirmed this decision on 2026-08-24.
- **Verification status:** repository scans and static diff checks confirm that the migrated histogram, scatter, and failure-marker actors no longer read positional timing/failure compatibility fields directly. Subsequent EVAL-04 testing exposed and corrected a nil-copy error in the no-failure Evaluation route; the project owner verified the corrected normal Evaluation route and accepted it on 2026-08-25. Extended coverage remains pending for no valid taps, one tap, misses only, early/late bias, disabled windows, FA+, automatically judged pump notes, negative chart offsets, stops/warps, very short and very dense charts, failure, rate changes, courses failed mid-song, more than 4,000 timing events, D3D/OpenGL renderers, and agreement between displayed statistics and independent fixture calculations.

### EVAL-04 — Screenshot capture and evaluation exit/input handling

- **Behavioral responsibility:** cycles independently available evaluation panes for single, double, versus, and shared-side styles; exposes pad events to the input-test pane when dedicated menu buttons are enabled; saves screenshots for the requesting player; and animates a render-to-texture copy of the evaluation screen when supported. Favorite shortcuts are handled independently. In keyboard-enabled Event Mode, Ctrl+R replays the song and Ctrl+P enters practice, while event overlays can temporarily replace the standard callbacks.
- **Important dependencies:** `SCREENMAN` input callbacks and redirects, screen actor names and `ExpandForDouble` commands, profile-selected pane defaults, `PREFSMAN` menu-button/Event Mode/display settings, `SaveScreenshot()`, UTF-8 title helpers, localized month names, render-to-texture support, `VOLT26.EvaluationInput`, favorites handling, `Branch.GameplayScreen()`, `ScreenPractice`, and online/event actors that request input ownership.
- **State and persistence:** available-pane selection and callback ownership are screen-local; configured starting panes come from profile options and fall back to the first pane that actually exists. Screenshots are durable files under the requesting profile when persistent, otherwise under the machine profile, using a `VOLT26/<year>/<localized month>/` prefix. The render texture remains transient VOLT26 session state. Favorite changes persist through the favorites subsystem. Replay and practice leave the evaluated stage complete and begin a new route.
- **VOLT26 delta:** on `codex/standalone-evaluation-input`, `VOLT26.EvaluationInput` owns available-pane preference resolution, named callback registration/cleanup, replay/practice eligibility, direct screenshot input, screenshot naming/capture, and transient screenshot textures. Pane navigation now uses actual pane numbers rather than assuming every optional pane exists. Screenshot capture reports both success and failure and writes under the VOLT26 namespace without relying on the inherited screen-code detector. Favorites and diagnostic input are separate actors/callbacks, while event-overlay handoff suspends all standard Evaluation callbacks and restores them through one controller. The inherited exit handler was removed.
- **Decision:** `Adapt` — retain pane navigation and screenshots, use a VOLT26-owned storage path and explicit success/failure feedback, isolate favorites and diagnostic input, centralize callback ownership and cleanup, and allow Ctrl+R/Ctrl+P only with keyboard features in non-course Event Mode. The project owner approved this decision on 2026-08-25.
- **Verification status:** static scans and diff checks confirm that Evaluation callback registration is centralized, screenshot state no longer uses direct compatibility access, and screenshot capture no longer handles favorites. A hidden bootstrap with `D:\SimplyLoveVanilla\Program\ITGmania.exe` loaded VOLT26 without Lua errors. Interactive testing confirmed that P2 Select writes one screenshot per press to the persistent profile's VOLT26 directory and presents the corrected feedback/animation; the project owner accepted EVAL-04 on 2026-08-25. Extended coverage remains pending for every available/missing pane in single, double, versus, routine, and shared-side play; dedicated and gameplay-button navigation; invalid profile pane values; screenshots to memory-card, guest, and unwritable storage; non-ASCII and symbol-heavy titles; renderer support on/off; favorite shortcuts; event-overlay handoff; Ctrl+R/Ctrl+P eligibility; and callback cleanup after every exit.

## Online services assessment

### ONLINE-01 — GrooveStats, QR workflows, and online lobbies

- **Behavioral responsibility:** negotiates the GrooveStats service capabilities at the start of a session; loads and saves per-profile API credentials and remotely synchronized player options; validates score eligibility; fetches scores and leaderboards; builds QR score and QR-login payloads; submits eligible pad scores; reports personal/world records; and starts event-unlock downloads returned by the service. A separate singleton WebSocket handler searches and joins online lobbies, synchronizes song and screen state, publishes live score/judgment state, blocks input while peers converge, and allows a held Start input to disconnect.
- **Important dependencies:** ITGmania `NETWORK:HttpRequest`, JSON and query encoders, `CRYPTMAN`, profile directories and `GrooveStats.ini`, `VOLT26.GrooveStats`, `VOLT26.Downloads`, chart hashes from `SELECT-04`, score/judgment state from `GAME-01` through `GAME-03`, evaluation panes and actor names, screen/input lifecycle messages, QR encoding actors, theme/operator preferences, service response schemas, and the fixed GrooveStats HTTPS host. Dormant event responses also call the `EVENT-01`, `EVENT-02`, and `SERVICE-02` boundaries.
- **State and persistence:** API keys and usernames persist in each loaded profile's `GrooveStats.ini`; the server can overwrite an allowlisted subset of player options that later persist through profile handling. Service availability, request caches, lobby readiness, active sockets, and download progress are transient. Successful unlocks are extracted under `/Songs/`, recorded in `/Songs/unlocks-cache.json`, and may create `Pack.ini`; QR score payloads expose score data through a user-scanned URL rather than writing locally.
- **VOLT26 delta:** on `codex/standalone-groovestats`, `VOLT26.GrooveStats` owns capability reset/negotiation, bounded response decoding, normalized per-profile identity persistence, and service eligibility. The request actor accepts only relative query endpoints on the fixed HTTPS API host, bounds timeouts and request/response bodies, isolates callbacks, and rejects malformed remote option data and unsafe numeric values. The Service Menu exposes only the master GrooveStats toggle; the Song Select leaderboard action is restored. Session and submission consumers ignore event/tournament payloads. The plaintext QR-login WebSocket, automatic archive downloads, and experimental lobby handler are not routed or instantiated.
- **Decision:** `Adapt` — retain the optional HTTPS score lookup, leaderboard, QR-score, and eligible pad-submission workflows. Defer QR login, event unlock downloads, and online lobbies until their separate transport, archive, and synchronization trust boundaries are accepted and verified. The project owner had explicitly requested GrooveStats support before this block began.
- **Verification status:** a static security and reachability contract passed 20 of 20 checks covering owned policy, menu exposure, HTTPS endpoint pinning, size/method/endpoint bounds, response and remote-option validation, and isolation of QR login, lobbies, downloads, and event payloads. A live request confirmed the expected official service capabilities, and the project owner verified persistent-profile authentication plus live leaderboard display on 2026-08-25. Eligible submission, offline/partial service, malformed responses, two-player, keyboard, course, and callback-exit cases remain extended coverage.

## Event and tournament assessment

### EVENT-01 — Event progress and evaluation interaction

- **Behavioral responsibility:** consumes RPG and ITL data returned by score submission, presents current score/point deltas, quest completion, unlocks, leaderboard entries, and per-event progress, and temporarily takes ownership of evaluation input so players can page through event result panes before returning to the normal evaluation flow. Compact progress summaries are also populated beside the standard evaluation result area.
- **Important dependencies:** successful `ONLINE-01` submission responses and their RPG/ITL schemas, GrooveStats date parsing, evaluation actor names and input callback arbitration from `EVAL-04`, current song/banner data, profile/player side mapping, optional SRPG10 globals and assets, widescreen/player-count layout rules, and event-specific strings, colors, and pane commands.
- **State and persistence:** the overlay and compact summaries are screen-local views of the current server response. Quest unlock downloads and ITL folder unlocks delegate persistence to `ONLINE-01` and `EVENT-02`; this actor does not independently store progress. Its input ownership is transient but can block normal evaluation navigation until explicitly released.
- **VOLT26 delta:** on `codex/event-result-boundary`, `VOLT26.Events` accepts optional RPG and ITL memberships from the bounded official GrooveStats submission response, normalizes event names and result identifiers, and rejects non-table or unnamed payloads before they reach Evaluation. Both memberships remain available simultaneously. Existing progress and overlay presentation stays dormant when no valid event is returned. Automatic archive downloads and the hard-coded local ITL 2026 persistence path remain isolated.
- **Decision:** `Adapt` — retain future RPG and ITL result/progress support as an optional consumer of validated online responses, as requested by the project owner. Do not tie the generic result boundary to a specific annual event or enable archive downloads implicitly.
- **Verification status:** static contracts cover accepted/rejected payload routing, simultaneous RPG/ITL preservation, absence of automatic-download reachability, local-ITL write guards, and centralized Evaluation input ownership. The project owner accepted this dormant future-event boundary on 2026-08-25. Live presentation remains extended coverage pending an active official event or representative fixtures.

### EVENT-02 — Tournament mode and local ITL records

- **Behavioral responsibility:** tournament preferences force a selected EX/ITG scoring display, control step-stat visibility, keep the FA+ pane available, and optionally replace CMods with an MMod on charts marked `no cmod`. Independently, the ITL subsystem recognizes 2026 event songs, validates eligible 1.0x dance scores using GrooveStats rules plus mines and timing-window requirements, calculates clear types and event points, preserves the best result by chart hash, maps song paths to hashes, records unlocked folders, and exposes local ITL data to the wheel and evaluation.
- **Important dependencies:** `EnableTournamentMode`, `ScoringSystem`, `StepStats`, and `EnforceNoCmod` preferences; player-option construction; gameplay step-stat and versus actors; title/subtitle metadata; chart hashes and EX/judgment calculations; `ValidForGrooveStats()`; profile directories; JSON/file APIs; current ITL pack naming; evaluation lifecycle; and unlock folders returned through `ONLINE-01`.
- **State and persistence:** tournament configuration persists as theme preferences and changes the available player-option surface. Per-player `pathMap`, `hashMap`, and `unlockFolders` are loaded from and written to `ITL2026.json` in an explicit profile; guest play has no durable ITL record. Event activity is currently hard-coded to always true, and year/pack/file identifiers are embedded in the implementation.
- **VOLT26 delta:** on `codex/adapt-tournament-mode`, `VOLT26.Tournament` owns the enabled state, enforced scoring display, and `no cmod` metadata detection. The Service Menu exposes the three safe generic settings. On `no cmod` songs, a selected CMod is synchronized to an equivalent MMod across VOLT26, preferred, stage, and song option state before the NoteField is constructed. Gameplay displays the forced conversion, and music selection restores the previous CMod afterward. Tournament Mode preserves the player's gameplay-statistics selection because forcing the inherited full pane breaks the VOLT26 gameplay layout. Annual ITL profile reads, writes, and Evaluation updates are disabled behind an explicit provider boundary; the hard-coded always-active 2026 implementation remains unavailable.
- **Decision:** `Adapt` — retain a small, explicitly specified generic tournament-policy layer. Keep annual event scoring disabled until it becomes a versioned provider with validated configuration, active dates, atomic persistence, and corrected best-score comparison.
- **Verification status:** static policy checks passed for menu reachability, pre-NoteField synchronization, temporary-state restoration, statistics-layout preservation, and annual-ITL isolation. The project owner verified the Tournament Service Menu, normal VOLT26 gameplay layout, visible forced CMod-to-MMod conversion on `[13] Testify`, and correct gameplay scrolling on 2026-08-25. Annual ITL fixtures remain deferred with the provider.

## Removable-media content assessment

### CONTENT-01 — Custom songs from USB profiles

- **Behavioral responsibility:** exposes ITGmania engine preferences for enabling memory cards and profile-hosted custom songs, maximum song count, load timeout, duration, and size. The theme hides the USB options when the engine does not provide `CustomSongsEnable`; actual discovery, validation, loading, limits, and cleanup of removable-media songs are engine responsibilities.
- **Important dependencies:** ITGmania memory-card/static-mount configuration, `PREFSMAN` support for `MemoryCards`, `CustomSongsEnable`, `CustomSongsMaxCount`, `CustomSongsLoadTimeout`, `CustomSongsMaxSeconds`, and `CustomSongsMaxMegabytes`, the service-option framework from `OPTIONS-02`, profile selection/reload behavior from `PROFILE-01`, and operating-system mount permissions.
- **State and persistence:** all settings are engine preferences and survive restart through ITGmania. Song files remain on removable profile media and are never copied, deleted, mounted, or scanned by theme code. Suggested timeout, duration, and size values remain available, while any positive engine-defined value outside those lists is preserved and can be saved unchanged.
- **VOLT26 delta:** on `codex/adapt-custom-songs`, `VOLT26.CustomSongs` owns the engine-capability check used by the Service Menu. The operator rows preserve valid custom numeric preferences instead of falling back to the first choice. The setup guide now describes VOLT26 on ITGmania, the required profile `Songs` directory, the six operator controls, and the engine/theme responsibility boundary.
- **Decision:** `Adapt` — retain the supported ITGmania operator controls, preserve engine-defined values, and document mounting plus song sandboxing as engine/operator prerequisites. Keep discovery, validation, loading, limit enforcement, failure reporting, and cleanup in the engine.
- **Verification status:** eight static contracts passed for capability-gated menu exposure, all six preference rows, preservation of custom timeout/duration/size values, numeric writes, current documentation, and the absence of theme-owned removable-media filesystem operations. A hidden ITGmania bootstrap loaded `ScreenUSBProfileOptions` without errors, and the project owner verified preference persistence on 2026-08-25. Real mounted profiles, malformed media, and removal during scanning remain engine-level extended coverage.

## Service and maintenance assessment

### SERVICE-01 — Input diagnostics and pad testing

- **Behavioral responsibility:** provides a service screen that displays attached devices, mapped game inputs, and currently held unmapped device inputs; draws dance, pump, techno, and dance-solo pad highlights; falls back to the engine input list for other games; and reuses the same pad actor in Event Mode music selection and evaluation diagnostics. Double and routine modes match events by controller rather than player number.
- **Important dependencies:** raw input callbacks, `Def.DeviceList`/`Def.InputList`, game/style/controller enums, `TestInputEvent` broadcasts, dedicated-menu-button and `AllowDanceSolo` preferences, localized labels, screen metrics, and input-routing coordination with `SELECT-02` and `EVAL-04`.
- **State and persistence:** held unmapped inputs and highlight visibility are screen-local and are not persisted. Input mappings are engine-owned and read-only here. Callback registration is explicit on the service screen and through parent overlay handlers elsewhere.
- **VOLT26 delta:** on `codex/adapt-input-diagnostics`, `VOLT26.InputDiagnostics` owns supported-game detection, repeat filtering, dismissal policy, player/controller routing, physical-source keys, reference-counted held state, and deterministic unmapped-input labels. Dance, pump, and techno retain pad renderers; para and kb7 retain the engine input list. Service-screen callbacks are explicitly removed on exit, Song Select exposes the accepted Event Mode diagnostic action, and Service, Song Select, and Evaluation clear held highlights when their diagnostic surface closes.
- **Decision:** `Adapt` — retain one shared VOLT26 input-state policy with the existing game-specific renderers and engine fallback. Treat mappings as read-only engine state, route double/shared styles by controller, and preserve every held physical source until its own release.
- **Verification status:** ten static contracts passed for the shared capability boundary, accepted Song Select action, callback removal, controller routing, physical-source tracking, repeat filtering, reset broadcasts, and shared consumer policy. Hidden ITGmania bootstraps loaded both `ScreenTestInput` and `ScreenSelectMusic` without Lua or missing-file errors. The project owner accepted the interactive Service and Event Mode diagnostic routes on 2026-08-25. Extended coverage includes every supported game/style, multiple devices, disconnects, late join, and Evaluation diagnostics with dedicated menu buttons.

### SERVICE-02 — Event-download status and navigation

- **Behavioral responsibility:** snapshots the transient `SL.Downloads` registry on entry, presents up to six scrollable items with progress, byte counts, completion and errors, refreshes every 0.1 seconds while work remains, and returns to music/course selection on Start, Back, or Select. The screen is reachable from music selection only when GrooveStats is connected and automatic unlock downloads are enabled.
- **Important dependencies:** download records and HTTP callbacks created by `ONLINE-01`, `Consensual-sick_wheel.lua`, named candidate actors, input callbacks, localized status strings, song/course branching, and the assumption that the download table's size remains constant while the screen is open.
- **State and persistence:** the viewer writes no durable state. `VOLT26.Downloads.Registry` holds transient provider jobs, while `Snapshot()` returns sorted normalized copies with bounded byte counts and explicit queued/downloading/verifying/extracting/completed/failed/cancelled states. The viewer rebuilds its candidates from current snapshots, so additions and removals no longer depend on the entry count captured at screen creation.
- **VOLT26 delta:** on `codex/adapt-download-viewer`, `VOLT26.Downloads` owns the dormant transport-neutral registry, normalization, immutable snapshots, and completion counts. The viewer handles empty and changing registries, clamps progress, refreshes its scroller, removes its input callback on exit, accepts navigation only on first press, and returns through VOLT26 navigation. The inherited producer points at the owned registry, but automatic event transport remains unreachable and `IsTransportEnabled()` is fixed false.
- **Decision:** `Adapt` — retain the status surface for a future validated event provider without authorizing network requests, archive extraction, retry, cancellation, or song reload. Those operations must be introduced with the provider's separate transport and filesystem security review.
- **Verification status:** eleven static contracts passed for transport isolation, action unreachability, owned registry, immutable normalization, bounded progress, explicit states, dynamic snapshots, callback cleanup, first-press input, VOLT26 navigation, and producer ownership. A hidden ITGmania bootstrap loaded `ScreenViewDownloads` with an empty registry without Lua or missing-file errors. The project owner accepted the dormant viewer boundary on 2026-08-25. Fixture coverage remains pending for active jobs, mutations, long labels, failures, retry/cancel, and controlled post-install reload when a provider is authorized.

## Session accounting assessment

### SESSION-01 — Gameplay duration and game-over statistics

- **Behavioral responsibility:** timestamps each player's gameplay actor on entry and stores elapsed screen time into the current per-stage snapshot on exit. Game Over sums available stage durations, counts played stages and non-miss per-column tap judgments, and shows session totals. Persistent profiles additionally show display name, lifetime songs played, avatar, and calories burned today unless the profile is configured to ignore engine calorie estimates.
- **Important dependencies:** per-stage storage from `EVAL-01`, per-column judgments from `GAME-02`, player-specific gameplay actor lifecycle, `GetTimeSinceStart()`, profile statistics and avatars, localized duration labels, late-join gaps, and the `ScreenGameOver` branch. The elapsed value measures time in the gameplay screen, not chart/music time.
- **State and persistence:** per-stage duration and derived session counts are transient in `SL[pn].Stages.Stats`; lifetime songs, profile name, calorie totals, avatar selection, and the ignore-calories flag are profile-owned persistent data. No separate session history is written by these actors.
- **VOLT26 delta:** completed on `codex/standalone-session-accounting`. `VOLT26.Session` owns monotonic per-player timers, separate screen and active seconds, pause/resume transitions, finished/completed state, sparse-stage aggregation, normalized tap-hit totals, rounded duration components, and profile-summary snapshots. Gameplay and Game Over actors are thin lifecycle/presentation adapters and no longer read compatibility session tables directly. New records exclude unfinished/aborted attempts from Game Over totals; legacy records without the versioned session object retain compatibility aggregation.
- **Recommended decision:** `Adapt` — retain lightweight game-cycle summaries, but define explicit session/stage records with monotonic start, pause/resume, completion, and abort semantics; derive tap totals from named normalized judgments; and label screen time versus active chart time accurately. Keep profile-owned lifetime and health-related data visually and semantically distinct from transient session totals.
- **Verification status:** static scans passed. Two engine-hosted contract runs passed 19 of 19 timer, pause/resume, finished/completed, Evaluation-before-Off completion, aborted-stage exclusion, sparse/legacy-stage, normalized-tap, duration-rounding, immutable-snapshot, and 1P/2P isolation checks. A clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. Interactive Game Over presentation plus live fail, give-up, Back/Escape, restart, sync prompt, replay/practice, courses, late join/unjoin, routine/shared-side play, profile/guest/memory-card variants, and ignored-calorie presentation remain extended coverage.

## Explicitly out of scope for this inventory

The following are tracked elsewhere unless they contain behavior required by an item above:

- colors, fonts, logos, backgrounds, decorative animation, and sound selection;
- actor coordinates, sizing, tweening, and aspect-ratio-specific layout;
- judgment and hold graphics as image assets;
- visual-style themes that do not affect navigation, state, scoring, persistence, or integration behavior.

## Upstream review log

| Review date | Simply Love tag/commit | VOLT26 branch | Result |
| --- | --- | --- | --- |
| Pending | Pending baseline identification | `codex/standalone-migration-plan` | Initial inventory structure created; detailed audit not started |
| 2026-08-24 | Pending baseline identification | `codex/baseline-runtime-stabilization` | Reviewed mixed runtime/presentation blockers in the local prototype; no standalone feature-adoption decision finalized |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/simply-love-functional-scan-core` | Completed the source-level scan for `CORE-01` through `CORE-05`; retention decisions and runtime verification remain pending |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/simply-love-functional-scan-profile-options` | Completed the source-level scan for `PROFILE-01`, `OPTIONS-01`, and `OPTIONS-02`; retention decisions and runtime verification remain pending |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/simply-love-functional-scan-select` | Completed the source-level scan for `SELECT-01` through `SELECT-05`; retention decisions and runtime verification remain pending |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/simply-love-functional-scan-gameplay` | Completed the source-level scan for `GAME-01` through `GAME-08`; retention decisions and runtime verification remain pending |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/simply-love-functional-scan-evaluation` | Completed the source-level scan for `EVAL-01` through `EVAL-04`; retention decisions and runtime verification remain pending |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/simply-love-functional-analysis-complete` | Completed the remaining source-level scan for `ONLINE-01`, `EVENT-01`, `EVENT-02`, `CONTENT-01`, `SERVICE-01`, `SERVICE-02`, and `SESSION-01`; the initial Phase 1 source analysis now covers every inventory item, while owner decisions and runtime verification remain pending |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-core-foundation` | Began the approved standalone implementation for `CORE-01` through `CORE-04`; introduced VOLT26-owned identity and APIs, migrated startup/title behavior away from Visual Style selection, and moved VOLT26 assets to a theme-owned path. A local ITGmania smoke test reached `ScreenTitleMenu` without Lua errors or missing theme elements; full route and compatibility-matrix verification remain pending. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-music-selection` | Completed the approved `SELECT-01` vertical slice; added the VOLT26 music-selection controller and made the existing VOLT26 ScreenSelectMusic composition, background, wheel metrics, and native rows standalone. Hidden loading and owner verification of the normal one- and two-player routes passed; the Song Select presentation remains intentionally work in progress until the standalone migration is complete. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-gameplay-evaluation` | Completed the minimal gameplay/evaluation vertical slice; introduced VOLT26-owned stage lifecycle and result-context APIs, connected the existing entry animations directly, removed production gameplay/evaluation `VisualStyle` reads, and made the failure spinner persistent without extending the screen transition. Owner pass/fail tests and the final runtime log verified the normal route without Lua errors. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-options` | Completed the player-facing options slice; introduced VOLT26-owned modifier access and three-page return routing, migrated the dynamic Player Options overlay, and connected screen metrics to the new controller. Bootstrap and interactive owner verification passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-gameplay-telemetry` | Completed the gameplay telemetry foundation; centralized EX, timing-offset, and per-column judgment capture in VOLT26-owned services while retaining compatibility fields for Evaluation. Bootstrap and interactive owner verification of scoring, result data, and offset graphs passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-gameplay-failure` | Completed standalone failure handling; centralized failure positions, stream annotations, give-up detection, autoplay tracking, course early-exit policy, and player-stat reconciliation. Bootstrap and owner verification of normal completion and intentional failure passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/evaluation-banner-background` | Aligned the Evaluation Stage background with the existing VOLT26 Song Select background while preserving evaluation content, transitions, and screenshot support. Hidden bootstrap verification passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/title-menu-afk-easter-egg` | Added the title-menu AFK easter egg with a three-minute inactivity policy that starts after menu arrival, synchronized looping video and extracted OGG audio, safe input dismissal, and menu-audio restoration. Hidden bootstrap and interactive owner verification passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/remove-mementos-dash` | Removed Mementos Dash from the product surface and repository, including its title-menu route, screen implementation, persistent high score, localization, and dedicated graphics. Repository scans, hidden bootstrap, and interactive owner verification of the remaining four title-menu choices passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/volt26-identity-inventory` | Reconciled the capability map with accepted standalone branches: `PROFILE-01`, `OPTIONS-01`, `SELECT-01`, `GAME-02`, `GAME-03`, `GAME-04`, and `EVAL-01` now record their implemented normal-route slices; `GAME-01` records its partial scoring migration. Extended-coverage verification and all remaining `Assess` decisions are still pending. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-song-browsing` | Confirmed `Adapt` decisions for `SELECT-02`, `SELECT-03`, `SELECT-04`, `EVAL-02`, and `EVAL-03`; completed `SELECT-02` by introducing the VOLT26 song-browsing service, consolidating search and preferred-sort behavior, and limiting the Sort Menu to accepted capabilities. Static checks and interactive owner verification passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-favorites` | Completed `SELECT-03` by replacing the inherited global favorites handler with `VOLT26.Favorites`, importing legacy `favorites.txt` into a namespaced store, and migrating all music-selection and evaluation consumers. Static checks and interactive owner verification passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-chart-metadata` | Completed and verified the `SELECT-04` boundary with VOLT26-owned chart snapshots and stream analysis, isolated optional hashing behind an explicit interface, removed inherited helper globals, stopped normal music browsing from parsing simfiles for online hashes, and synchronized graph, label, and meter colors with the default Simply Love difficulty palette. Static checks and interactive owner verification passed. |
| 2026-08-24 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/standalone-evaluation-results` | Completed and verified the EVAL-02 normalized result and record-policy boundary, migrated Evaluation calculations and compatibility storage to `VOLT26.Results`, and retained the existing presentation and summary layout. Static checks, a hidden VOLT26 bootstrap, and interactive owner verification passed. |
| 2026-08-24 | `fdf6aa839cc768c2ee115dbde9737c5459edbf13` | `codex/standalone-evaluation-analysis` | Completed EVAL-03 with VOLT26-owned timing statistics, histogram smoothing, bounded scatter batches, shared song/course coordinates, and named failure snapshots. Static checks passed, the running VOLT26 log contained no Lua errors, and the project owner accepted the interactive result on 2026-08-25. |
| 2026-08-25 | `778e2f199162a9b3e7ead5f3b4537885a4ef4c67` | `codex/standalone-evaluation-input` | Completed EVAL-04 with available-pane selection, centralized callback ownership, direct and debounced VOLT26 screenshot capture/feedback, isolated favorites/diagnostics, and explicit replay/practice eligibility. Static checks, a hidden VOLT26 bootstrap, runtime log inspection, and interactive owner verification passed. |
| 2026-08-25 | `a22497e1a8dac1262a5e98c894646f4fe8033ba7` | `codex/standalone-core-completion` | Completed and verified CORE-01 through CORE-04 with guarded runtime initialization, a VOLT26 compatibility contract, owned preference validation and legacy import, managed engine-preference policy, and a VOLT26 navigation graph with thin engine adapters. An engine-hosted contract run passed 19 of 19 reset, preference, compatibility, adapter, and one-/two-player route checks; a clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. |
| 2026-08-25 | `26c325d7b47fbaec33ae27d23d4de359a3b8238c` | `codex/standalone-gameplay-core-completion` | Completed and verified GAME-01 through GAME-04 with VOLT26-owned scoring, immutable score updates, normalized judgment telemetry, bounded failure policy, and versioned per-profile score export. An engine-hosted contract run passed 21 of 21 deterministic scoring, telemetry, failure, isolation, and JSON checks; a clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. |
| 2026-08-25 | `fc93e8641c5b0a60509aab2274c5e8709528b8d5` | `codex/standalone-session-accounting` | Completed and engine-verified SESSION-01 with monotonic screen/active timers, pause accounting, completed-stage policy, normalized tap totals, sparse-stage aggregation, and profile-summary snapshots. Two engine-hosted contract runs passed 19 of 19 deterministic lifecycle, aggregation, formatting, completion-order, and one-/two-player checks; a clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. |
| 2026-08-25 | `422f7575` | `codex/standalone-implemented-boundary-completion` | Completed and engine-verified PROFILE-01, OPTIONS-01, SELECT-01 through SELECT-03, and EVAL-01. Added normalized music-rate handling, versioned and immutable Evaluation contexts/player snapshots, explicit active-stage identity, idempotent completion, and VOLT26-owned Summary/Name Entry consumption. A combined engine-hosted contract passed 26 of 26 boundary, parsing, policy, immutability, 1P/2P isolation, and lifecycle checks; a clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors. |
| 2026-08-25 | `4d5fdc52` | `codex/standalone-gameplay-advanced-completion` | Completed and verified GAME-05 through GAME-08 with VOLT26-owned gameplay-stat, target-score, course-speed, and versus services; migrated active actors away from direct Simply Love gameplay state while preserving presentation and optional online boundaries. An engine-hosted contract passed 35 of 35 deterministic fixtures, a clean follow-up bootstrap reached `ScreenTitleMenu` without Lua errors, and the project owner accepted the live block on 2026-08-25. |
| 2026-08-25 | `6ba5de55` | `codex/standalone-core-utilities` | Completed and verified CORE-05 with VOLT26-owned utility, UTF-8 text, metadata, and renderer-capability services; renamed the utility/text/UTF-8 modules, removed the inherited support-helper module, and migrated accepted system-message and emoji consumers. An engine-hosted contract passed 25 of 25 deterministic fixtures, a clean bootstrap reached `ScreenTitleMenu` without Lua errors, and the project owner accepted the live block on 2026-08-25. |
| 2026-08-25 | `4e6bb8df` | `codex/standalone-operator-options` | Completed and verified OPTIONS-02 with VOLT26-owned operator-menu policy, engine-preference rows, active-row restoration, environment validation, and callback cleanup. Optional online, tournament, Casual Mode, alternate wheel-style, image-cache, and visual-style rows are no longer reachable from the curated service hierarchy. An engine-hosted contract passed 30 of 30 deterministic policy and actor-loading fixtures, a clean bootstrap reached `ScreenTitleMenu`, and the project owner accepted the live block on 2026-08-25. |
| 2026-08-25 | `7ec91085` | `codex/omit-casual-mode` | Completed and verified the owner-approved SELECT-05 omission: removed the dedicated Casual screen, configuration, documentation, preferences, service/sort actions, and navigation branches; normalized stale Casual state to ITG; and retained only unreachable compatibility conditions for the final adapter-removal pass. Static repository contracts passed, hidden ITGmania launches remained responsive, and the project owner accepted the normal route on 2026-08-25. |
| 2026-08-25 | `99e721e1` | `codex/standalone-groovestats` | Implemented ONLINE-01's accepted HTTPS GrooveStats boundary with strict capability parsing, normalized profile identity, bounded requests/responses and remote options, restored operator and leaderboard entry points, and isolated QR login, automatic downloads, lobbies, and event payloads. A 20-check static contract and the live official session-capability request passed; owner runtime verification is pending. |
| 2026-08-25 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/adapt-tournament-mode` | Implemented and owner-verified the generic Tournament Mode boundary with EX/ITG display enforcement, safe `no cmod` MMod forcing before NoteField construction, CMod restoration, preserved player statistics layout, and isolated annual ITL persistence. |
| 2026-08-25 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/adapt-input-diagnostics` | Completed and owner-verified SERVICE-01 with a shared input-diagnostic policy, reference-counted physical sources, explicit callback cleanup, Event Mode Song Select access, and retained engine fallback for unsupported visual games. Ten static contracts and hidden Service/Song Select bootstraps passed. |
| 2026-08-25 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/adapt-download-viewer` | Completed and owner-accepted SERVICE-02 with a dormant transport-neutral job registry, immutable normalized snapshots, dynamic viewer updates, callback cleanup, and VOLT26 return navigation. Automatic event transport remains disabled pending its own security review. Eleven static contracts and an empty-registry hidden bootstrap passed. |
| 2026-08-25 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/close-functional-inventory` | Reconciled all capability statuses with accepted scope and completed owner tests. The standalone functional inventory now has explicit decisions and verified boundaries for every listed capability; live annual-event providers, automatic downloads, online lobbies, and extended device/media variants remain outside the active release boundary. |
| 2026-08-26 | `dd06138b15492f4136796dfe4b6708ced0f7b9eb` | `codex/paid-play-attract` | Extended CORE-04 with a zero-credit paid-play attract route from recent machine scores to ITGmania's native random-song CPU demonstration, while excluding the five-minute AFK video from paid play. When the standard timer preference is enabled, the countdown now renders above custom Song Select and options layers; disabling the preference still disables the timer. A forced engine smoke test traversed the complete attract route and loaded the isolated CPU demonstration without Lua errors; the owner verified the live attract, CPU presentation, and enabled timer visibility. |
| 2026-08-30 | No upstream sync; VOLT26-owned presentation extension | `codex/song-select-reset`, `codex/fix-theme-reload-banners`, `codex/fix-cjk-right-metadata` | Rebuilt Song Select around the approved three-column mockup with a responsive one-/two-player information layout, the supplied `bg_ss@2x.png` background, native vertical wheel rows, supplied Select artwork, and a timing-aware transparent CEL chart preview. Follow-up owner feedback hid the preview on group rows, restored readable group labels, applied beat-derived rhythm colors, substituted profile names for player-number labels, separated compact two-player panel text, set the full-height wheel rail and circular markers to the approved red palette, increased banner-to-metadata spacing, and removed navigation hitches through immediate wheel settling plus cached incremental sample-window parsing. The focused-row connector spans from the enlarged wheel marker to the artwork. Song and pack artwork preserve source aspect ratio through centered cover-cropping; restrained red strokes are reserved for pack artwork so groups remain distinguishable from songs. Pack rows share the song-row grammar, carry an explicit pack/count label, fall back to artwork from their first song, and use a red typographic PACK tile when no image exists. Focused packs reuse the song title/count actors beneath the artwork, while inherited section labels remain hidden. Compact song banners render as still frames so recycled rows cannot advance shared movie textures. The selected song uses one dedicated video actor that loads its banner directly, bypassing ITGmania's cross-theme banner cache so switching from Simply Love cannot retain stale movie state. Compact pack labels stay within the wheel column, and missing song artwork in the centered pack list receives a numbered fallback tile. Selecting a pack replaces the note preview with a centered song list on the same neutral surface as the other panels; nine rows remain visible while the complete group moves through the clipped viewport at the same continuous half-second-per-item pace as the ranking score scroller. Hold and roll rendering uses bottom-anchored repeating CEL bodies, short texture-matched triangular tails, and receptor-pinned active heads. The M600 preview preserves BPM changes and pauses, removes taps at the receptor, and triggers a short CEL impact pulse; guide rules are omitted and a vertically faded neutral surface separates the chart from the page. The Select logo and preview remain centered. Long song titles remain on one line and scale down uniformly to the metadata-column width instead of wrapping or stretching. Difficulty and player-name strips share the exact neutral surface used by the chart panels. Available difficulty meters sit in a dedicated bottom-center strip with per-player selection underlines. Player chart panels use borderless neutral dark surfaces with reduced coral and steel-blue player accents; their graph height is reduced so larger tech annotations and chart statistics carry more visual weight. Artist, chart-description, and step-author metadata automatically use the readable Common font whenever CJK text is present, both on the wheel and in the right-side information panels. Step type is omitted, and presentation metadata uses neutral dash separators instead of double slashes. The Select Music options/sort overlay keeps Helvetica but normalizes it to the legacy menu's intended glyph scale so rows, headings, and cancel text no longer overlap. A live restart verified that the normal `ScreenInit` cinematic route was restored after screenshot tooling was removed; interactive verification of banner playback after a Simply Love-to-VOLT26 theme switch remains pending. |
| 2026-09-01 | No upstream sync; VOLT26-owned Paid Mode presentation | `codex/paid-song-select-timer` | Replaced the top-center Select artwork on Paid Mode Song Select with a custom rendering of the engine-owned menu countdown using the VOLT26 combo-number font. The native timer remains authoritative but its standard top-right actor is hidden on that route. Paid Mode detection accepts both runtime enum and persisted preference forms, and the custom actor receives an immediate initial value plus explicit foreground draw order before mirroring the engine timer. The per-frame mirror runs on the containing ActorFrame and updates the referenced BitmapText, matching the theme's established update pattern. Paid Mode forces this Song Select timer even when the general machine Menu Timer is disabled, preserves the remaining time across auxiliary screens, and resets it after gameplay; non-paid presentation continues to follow the machine preference. Static source contracts passed; interactive Paid Mode countdown and expiry verification remain pending. |
| 2026-09-01 | No upstream sync; VOLT26-owned presentation refinement | `codex/song-select-wheel-motion` | Polished the native Song Select wheel for songs and packs with an 80 ms transition, continuous focused-row spacing, and restrained edge fading. Right-side song/chart details now refresh after 0.35 seconds of stable selection and return with a 0.2-second right-to-left slide and fade. The difficulty strip and complete pack-hover panel follow the same delay with a bottom-to-top entrance; delaying the pack refresh also prevents artwork for transient groups from loading while the wheel is moving. The chart-preview parser shares the delay so continuous scrolling does not read or cache transient charts, and its dark gradient backdrop reveals in sync with the chart. Static metric and Lua contract verification passed; interactive motion and rapid-input verification remain pending. |
