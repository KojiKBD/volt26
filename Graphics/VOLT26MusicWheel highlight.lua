-- Selection art is drawn by MusicWheelItemNative.lua so it can carry the
-- row's index, title, artist, and BPM as one irregular composition.
return Def.Quad{InitCommand=function(self) self:visible(false) end}
